% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_functional_richness.R
\name{compare_functional_richness}
\alias{compare_functional_richness}
\title{Compare bootstrap-based functional richness estimates across methods}
\usage{
compare_functional_richness(
  x,
  groups = NULL,
  methods = c("convexhull", "dendrogram", "tpd", "hypervolume"),
  n_axes = NULL,
  var_threshold = 0.98,
  n_boot = 100,
  log_transform = TRUE,
  scale = TRUE,
  alpha = 0.05,
  seed = NULL,
  ...
)
}
\arguments{
\item{x, groups, n_axes, var_threshold, n_boot, log_transform, scale}{As in
\code{\link[=bootstrap_functional_space]{bootstrap_functional_space()}}.}

\item{methods}{Character vector, one or more of \code{"convexhull"},
\code{"dendrogram"}, \code{"tpd"}, \code{"hypervolume"} (see
\code{\link[=bootstrap_functional_space]{bootstrap_functional_space()}} for what each measures). Defaults to
all four. A method whose Suggested package is not installed, or that
errors for any other reason (e.g. \code{n_axes} too large for
\code{"convexhull"}'s affine-independence requirement), is not fatal: it
is recorded as \code{status != "ok"} in \verb{$summary} with \code{NA} numeric
columns, and the comparison proceeds with the remaining methods.}

\item{alpha}{Numeric, the significance threshold used to flag
\verb{$summary$significant} and summarised in \code{print()}. Defaults to
\code{0.05}. Purely a display/summary convenience: the underlying
\code{p_value} for each method is always reported in full.}

\item{seed}{Optional single integer. If supplied, \code{set.seed(seed)} is
called immediately before \emph{each} method's \code{\link[=bootstrap_functional_space]{bootstrap_functional_space()}}
call, so every method draws the same sequence of one-individual-per-
species bootstrap "communities" (community 1 is the same draw under
every method, community 2 the same under every method, and so on) --
useful if you want the per-draw richness values to be directly
paired across methods (e.g. to correlate them), rather than merely
comparing summary statistics. Defaults to \code{NULL} (no explicit
seeding; each method's draws continue the ambient RNG stream, as a
single ordinary call to \code{\link[=bootstrap_functional_space]{bootstrap_functional_space()}} would).}

\item{...}{Further method-specific tuning arguments forwarded as-is to
every \code{\link[=bootstrap_functional_space]{bootstrap_functional_space()}} call: \code{dendrogram_linkage},
\code{tpd_alpha}, \code{tpd_bw_factor}, \code{tpd_n_divisions}, \code{hv_bw_method},
\code{hv_samples_per_point}. Irrelevant arguments are simply ignored by
whichever method does not use them.}
}
\value{
An object of class \code{"intrait_richness_comparison"}, a list
with elements: \code{summary} (a \code{data.frame}, one row per requested
method, in the order of \code{methods}, with columns \code{method}, \code{status}
(\code{"ok"} or a \code{"skipped: <error message>"} note), \code{n_axes},
\code{var_explained}, \code{fd_ref}, \code{fd_boot_mean}, \code{fd_boot_sd}, \code{diff},
\code{pct_diff} (\code{100 * diff / fd_ref}, the cross-method-comparable
quantity), \code{p_value}, and \code{significant} (\code{p_value < alpha}); \code{NA} in
every numeric column for a skipped method), \code{results} (a named list
of the full \code{"intrait_bootstrap_fspace"} object for every method that
succeeded, e.g. for \code{plot()}-ing an individual method's bootstrap
histogram), \code{n_boot}, and \code{alpha}. Has dedicated \code{\link[=print]{print()}} and
\code{\link[=plot]{plot()}} methods.
}
\description{
Runs \code{\link[=bootstrap_functional_space]{bootstrap_functional_space()}} once per requested \code{method}
(\code{"convexhull"}, \code{"dendrogram"}, \code{"tpd"}, \code{"hypervolume"}) on the same
data, and tabulates the results side by side. Convex-hull volume,
dendrogram branch length, TPD richness, and hypervolume are not on the
same scale, so raw \code{fd_ref}/\code{fd_boot_mean} values are not directly
comparable across rows; what \emph{is} comparable is whether each method
agrees, qualitatively, that individual-based richness exceeds the
centroid-based reference, and by roughly how much in relative
(percentage) terms -- exactly the triangulation this function is for.
}
\details{
Each method gets its own, independent call to
\code{\link[=bootstrap_functional_space]{bootstrap_functional_space()}} -- including its own fresh PCA and its
own \code{n_boot} bootstrap draws -- rather than sharing internal
computation across methods; this keeps the comparison exactly as
trustworthy as calling \code{\link[=bootstrap_functional_space]{bootstrap_functional_space()}} directly four
times, at the cost of repeating the (comparatively cheap) PCA step. Use
\code{seed} if you specifically want the same draws reused across methods
rather than four independent bootstrap samples.

\code{pct_diff} is the one quantity meaningfully compared across rows:
consistent, similarly sized, statistically significant \code{pct_diff}
across most or all methods is stronger evidence that intraspecific
trait variability genuinely inflates the estimated functional space
than a single method's result taken alone, since the four measures
make different assumptions (hard-edged hull vs. kernel-smoothed
density vs. distance-based dendrogram) and so are unlikely to agree
spuriously for the same reason.
}
\examples{
\donttest{
fish <- load_t26_saudrune_landmarks()
segments <- fishmorph_segments(fish)
ratios <- fishmorph_ratios(segments)
ts <- trait_space(ratios, groups = fish$metadata$species, na_action = "omit")

# "dendrogram" always runs; "convexhull"/"tpd"/"hypervolume" are
# skipped gracefully (not fatal) if their package is not installed
cmp <- compare_functional_richness(ts, n_axes = 2, n_boot = 100)
cmp
plot(cmp)
}
}
\references{
Bertrand P (2026). Intraspecific trait variability shapes the
functional space of freshwater fish in French Guiana assemblages. M2
Biodiversity Ecology Evolution (BEE) internship report, Lille
University / Centre de Recherche sur la Biodiversite et l'Environnement
(CRBE, AQUAECO team), unpublished, supervised by A. Toussaint and S.
Brosse.
}
\seealso{
\code{\link[=bootstrap_functional_space]{bootstrap_functional_space()}}, \code{\link[=species_sensitivity]{species_sensitivity()}}
}
