% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/landmark_journal.R
\name{consolidate_landmarks}
\alias{consolidate_landmarks}
\title{Rebuild the landmark table from the journals}
\usage{
consolidate_landmarks(
  journal_dir,
  points = 1:22,
  history = FALSE,
  xlsx_path = NULL
)
}
\arguments{
\item{journal_dir}{Journal directory, or a vector of directories.}

\item{points}{Landmark numbers to lay out as columns. Defaults to \code{1:22}
(the FISHMORPH scheme plus the curvature point; see
\code{\link[=digitize_landmarks]{digitize_landmarks()}}).}

\item{history}{Logical. \code{FALSE} (default) keeps only the last record per
key; \code{TRUE} keeps every record, adding \code{record_id} so the successive
corrections of one specimen can be told apart and compared. \code{timestamp} is
returned either way.}

\item{xlsx_path}{Optional path; when supplied, the result is also written
there with \code{\link[writexl:write_xlsx]{writexl::write_xlsx()}}, one sheet per group of \code{target_sheet}
(typically \code{measurements} and \code{bias}).}
}
\value{
A \code{data.frame}, one row per digitization, with the identification
columns (\code{specimen}, \code{individual}, \code{replicate}, \code{operator}, \code{photo_file},
...), the coordinate columns, and the per-record status counts
(\code{n_clicked}, \code{n_seeded}, \code{n_predicted}, \code{n_adjusted}, \code{n_na}).
}
\description{
Reconstructs, from every journal in \code{journal_dir}, one row per digitization
in the "wide" layout of the workbook written by \code{\link[=digitize_landmarks]{digitize_landmarks()}}
(\verb{1_X, 1_Y, ... 22_X, 22_Y}), keeping for each key the LAST record -- so a
specimen digitized, then corrected, appears once, corrected. This is the
recovery path: the workbook can be deleted, corrupted, or left behind by a
crashed session, and the data are still there.
}
\examples{
d <- file.path(tempdir(), "journal_demo4")
jr <- landmark_journal_open(d, operator = "AT")
P <- cbind(c(10, 60), c(20, 22))
landmark_journal_append(jr, "fish_01", P, 1:2, specimen = "fish_01",
                        individual = "fish_01", target_sheet = "measurements")
consolidate_landmarks(d, points = 1:2)[c("specimen", "1_X", "2_X")]
unlink(d, recursive = TRUE)

}
\seealso{
\code{\link[=landmark_journal_read]{landmark_journal_read()}}, \code{\link[=digitize_landmarks]{digitize_landmarks()}},
\code{\link[=read_landmarks_xlsx]{read_landmarks_xlsx()}}
}
