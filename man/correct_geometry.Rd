% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correct_geometry.R
\name{correct_geometry}
\alias{correct_geometry}
\title{Standardize landmark configurations to a common scale, orientation, and
geometric convention}
\usage{
correct_geometry(
  landmarks,
  specimen = NULL,
  scale_bar_pos = c(0.1, 0.1),
  tolerance_coord = 1e-06
)
}
\arguments{
\item{landmarks}{An object of class \code{"intrait_landmarks"}, or a raw
\verb{p x k x n} landmark array, with at least the 21 landmarks of the
FISHMORPH scheme (points 1-19 plus the scale bar, 20-21).}

\item{specimen}{\code{NULL} (default) to standardize every specimen, or an
integer/character vector to restrict this to a subset.}

\item{scale_bar_pos}{Numeric length-2 vector, the \code{c(x, y)} position (in
the post-rescaling \verb{[0, 1]} space) landmark 20 is moved to; landmark
21 is placed to its right, at \code{scale_bar_pos + c(length, 0)}, where
\code{length} is the scale bar's own original length scaled by the same
factor as the body (see Details) -- so the calibration ratio between
the scale bar and the body is preserved, only its position and
orientation are standardized. Defaults to \code{c(0.1, 0.1)} (bottom-left).}

\item{tolerance_coord}{Numeric, proportion of body length (Bl, the
distance between landmarks 1 and 2, now measured in the rescaled and
rotated frame) \emph{any} point in a landmark group is allowed to sit from
the shared coordinate the rest of the group agrees on, before it is
corrected in step (4) -- same meaning as \code{correct_landmarks(rule = "check_geometry")}'s own \code{tolerance_coord}, but a much smaller default
here (\code{1e-6}, i.e. essentially any visible misalignment is corrected):
\code{correct_landmarks(rule = "check_geometry")}'s \code{0.02} default is tuned
as a \emph{diagnostic} threshold (distinguishing routine digitization noise
from a problem worth a human's attention), whereas this function's job
is to actually produce a clean, standardized configuration, for which
there is no principled reason to leave a detectable misalignment
uncorrected. Raise it (e.g. to match \code{check_geometry()}'s \code{0.02}) to
preserve small deviations you consider acceptable digitization noise
instead of correcting them.}
}
\value{
An object of the same class as \code{landmarks}, with every
specimen's coordinates replaced by their standardized version. The
returned \code{coords} array carries three attributes:
\describe{
\item{\code{standardization_log}}{a \code{data.frame}, one row per specimen
processed, with columns \code{specimen}, \code{scale_factor} (the isotropic
factor applied in step 1), \code{rotation_deg} (the rotation applied in
step 3), \code{y_shift} (the vertical translation applied immediately
after that rotation to bring the axis to \code{Y = 0.5}), and
\code{scale_bar_placed} (logical, whether landmarks 20-21 were
repositioned). Merged with any pre-existing \code{standardization_log}
from an earlier call, so successive calls accumulate a full
record; note that \code{old_value}/\code{new_value} in any pre-existing
\code{correction_log} (see below) predating a given
\code{standardization_log} row are expressed in that earlier,
now-superseded coordinate frame.}
\item{\code{corrected}}{a \verb{p x n} logical matrix, \code{TRUE} where step (4)
moved that point; used by \code{\link[=plot_fishmorph_points]{plot_fishmorph_points()}} to highlight
corrected points in blue. Shared with, and merged across,
\code{\link[=correct_landmarks]{correct_landmarks()}} (\code{rule = "align"}) calls on the same object.}
\item{\code{correction_log}}{a \code{data.frame}, one row per point corrected
by step (4), with columns \code{specimen}, \code{check}, \code{landmark}, \code{axis},
\code{old_value}, \code{new_value}, \code{reference_points}, \code{reference_value} --
the same shape as \code{\link[=correct_landmarks]{correct_landmarks()}} (\code{rule = "align"})'s own
log, so both accumulate into one unified audit trail.}
}
}
\description{
A full geometric standardization pipeline for FISHMORPH landmark
configurations, applied to every specimen in a fixed order: (1) rescale
the body landmarks isotropically so they fit within \verb{[0, 1]} (preserving
body shape -- see Details); (2) reposition the embedded scale bar
(landmarks 20-21) to a fixed corner of that \verb{[0, 1]} space, as a clean
horizontal reference segment rather than wherever it happened to sit in
the original photograph; (3) rotate the body so the main axis (landmarks
1-2) is exactly horizontal, with landmark 1 always to the left of
landmark 2, then shift it vertically so that axis sits at \code{Y = 0.5} for
\emph{every} specimen; (4) correct, exactly as
\code{correct_landmarks(rule = "check_geometry")} audits, whichever of the
five landmark-coordinate-scatter conventions still fails to hold once
the specimen is in this canonical frame. Each step operates on the
output of the step before it, in this order, for every specimen.
}
\details{
\strong{Step 1 -- rescale to \verb{[0, 1]} (isotropic).} A single scale factor,
\code{1 / max(range(X), range(Y))} computed over the body landmarks (1-19,
plus 22 if present; the scale bar is excluded so its, often arbitrary,
position in the original photograph cannot distort the body's own
scale), is applied to \emph{both} axes, so body shape (e.g. the ratio of
body depth to body length) is preserved exactly -- an independent
per-axis rescaling would instead stretch the body and silently corrupt
every FISHMORPH ratio and any shape-based analysis (GPA, shape PCA)
computed afterwards. Consequently, only the longer of the two axes ends
up spanning the full \verb{[0, 1]} range exactly; if X is the shorter axis it
is centered within \verb{[0, 1]} at this stage (Y does not need centering
here, since step 3 re-anchors it to \code{Y = 0.5} regardless).

\strong{Step 2 -- reposition the scale bar.} Landmarks 20 and 21 (missing for
a given specimen, this step is skipped for it, with a warning) are
moved to \code{scale_bar_pos} and \code{scale_bar_pos + c(length, 0)}
respectively, where \code{length} is their original Euclidean distance
multiplied by step 1's scale factor -- i.e. the scale bar keeps the
same size \emph{relative to the body} (its calibration meaning is
preserved), it is simply drawn as a clean, purely horizontal reference
segment in a fixed corner rather than wherever, and at whatever angle,
it happened to be digitized.

\strong{Step 3 -- rotate the body horizontal, anchored at Y = 0.5.} The
whole body (landmarks 1-19, plus 22 if present; the now-repositioned
scale bar is a fixed legend element and is neither rotated nor shifted)
is rotated rigidly about landmark 1 by whatever angle brings the vector
from landmark 1 to landmark 2 exactly onto the positive X axis -- this
necessarily also places landmark 1 to the left of landmark 2, so this
step also standardizes left-right orientation as a side effect -- and
then translated vertically so that this now-horizontal axis sits at
\code{Y = 0.5} for \emph{every} specimen, so standardized configurations line up
at a common height for visual comparison. This step does \emph{not}
standardize dorsal-ventral orientation (whether landmark 4 sits below
landmark 3); pair this function with \code{\link[=standardize_orientation]{standardize_orientation()}} if a
specimen may also be digitized upside-down. Skipped, with a warning, if
landmark 1 or 2 is missing.

\strong{Step 4 -- correct remaining conventions.} The same landmark groups
\code{correct_landmarks(rule = "check_geometry")} audits (see its Details for
the full list and the two-point-segment anatomical-anchor rationale),
now applied in the canonical frame step 3 established, so "vertical"
and "horizontal" checks are evaluated directly on raw X/Y:
\code{perpendicular_seg_1_9_vs_axis}, \code{perpendicular_seg_3_4_vs_axis}, and
\code{perpendicular_seg_10_11_vs_axis} each snap the companion point's X onto
its anchor's X; \code{perpendicular_eye_vertical_vs_axis} snaps \emph{every}
point among 5, 13, 7, 14, 6, 8 that sits beyond \code{tolerance_coord} from
their shared median X onto that median (not only the single worst one,
since more than one of the six can be misplaced); \code{axis_horizontal_parallel}
does the same for points 9, 8, 11, 4 against their shared median Y. The
reference (anchor, or median) is computed once per group, before any
correction, so this fully resolves a group with several misplaced
points in a single pass; re-running the function is only needed if a
later specimen is re-digitized or otherwise changed afterwards. Skipped,
with a warning, if step 3 itself was skipped (an axis-relative
correction is not meaningful without a horizontal axis).

As with \code{\link[=correct_landmarks]{correct_landmarks()}}, \code{\link[=standardize_orientation]{standardize_orientation()}}, and
\code{\link[=impute_landmarks]{impute_landmarks()}}, this is not a substitute for re-digitizing from
the original photograph when that is possible, and every step is fully
logged (\code{standardization_log}, \code{correction_log}) for transparency.
Because it only ever rescales, translates, and rigidly rotates
coordinates -- and only ever corrects a coordinate onto another point's
already-present value -- running it twice on already-standardized data
is harmless (idempotent up to floating-point precision).
}
\examples{
fish <- load_t26_saudrune_landmarks()
# fix left-right/dorsal-ventral mirroring first (a separate concern from
# the rescaling/rotation/alignment this function handles):
fish_oriented <- standardize_orientation(fish)
fish_std <- correct_geometry(fish_oriented)
attr(fish_std$coords, "standardization_log")
attr(fish_std$coords, "correction_log")
# the scale bar now reads as a clean horizontal segment bottom-left,
# and points 9, 8, 11, 4 line up exactly horizontal:
plot_fishmorph_points(fish_std, specimen = "T-26-0010_Operator_1")

}
\seealso{
\code{\link[=correct_landmarks]{correct_landmarks()}}, \code{\link[=standardize_orientation]{standardize_orientation()}},
\code{\link[=plot_fishmorph_points]{plot_fishmorph_points()}}, \code{\link[=impute_landmarks]{impute_landmarks()}}
}
