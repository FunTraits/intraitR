% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correct_geometry_conventions.R
\name{correct_geometry_conventions}
\alias{correct_geometry_conventions}
\title{Actively correct landmarks that violate the FISHMORPH geometric
conventions, once the axis is horizontal}
\usage{
correct_geometry_conventions(
  landmarks,
  specimen = NULL,
  tolerance_coord = 1e-06
)
}
\arguments{
\item{landmarks}{An object of class \code{"intrait_landmarks"}, or a raw
\verb{p x k x n} landmark array, \strong{already standardized} so that the main
axis (landmarks 1-2) is horizontal -- i.e. the output of
\code{\link[=standardize_geometry]{standardize_geometry()}} or \code{\link[=correct_geometry]{correct_geometry()}}, not raw digitized
coordinates (see Details).}

\item{specimen}{\code{NULL} (default) to check/correct every specimen, or an
integer/character vector to restrict this to a subset.}

\item{tolerance_coord}{Numeric, proportion of body length (Bl, the
distance between landmarks 1 and 2) any point in a landmark group is
allowed to sit from the shared coordinate the rest of the group agrees
on, before it is corrected -- same meaning as
\code{correct_landmarks(rule = "check_geometry")}'s own \code{tolerance_coord},
but a much smaller default here (\code{1e-6}, i.e. essentially any visible
misalignment is corrected): see \code{\link[=correct_geometry]{correct_geometry()}}'s Details for the
full rationale. Raise it (e.g. to match \code{check_geometry()}'s \code{0.02})
to preserve small deviations you consider acceptable digitization
noise instead of correcting them.}
}
\value{
An object of the same class as \code{landmarks}, with every
flagged coordinate replaced by its group's reference value. The
returned \code{coords} array carries two attributes, both merged with any
pre-existing ones from an earlier \code{\link[=correct_geometry]{correct_geometry()}}/
\code{\link[=correct_landmarks]{correct_landmarks()}} call on the same object, so a full audit trail
accumulates across successive corrections:
\describe{
\item{\code{corrected}}{a \verb{p x n} logical matrix, \code{TRUE} where this
function moved that point; used by \code{\link[=plot_fishmorph_points]{plot_fishmorph_points()}} to
highlight corrected points in blue.}
\item{\code{correction_log}}{a \code{data.frame}, one row per point corrected,
with columns \code{specimen}, \code{check}, \code{landmark}, \code{axis}, \code{old_value},
\code{new_value}, \code{reference_points}, \code{reference_value} -- the same
shape as \code{\link[=correct_landmarks]{correct_landmarks()}} (\code{rule = "align"})'s own log.}
}
}
\description{
The value-\emph{changing} half of \code{\link[=correct_geometry]{correct_geometry()}}'s pipeline (its step
4), available on its own: corrects, for every specimen, whichever of the
five landmark-coordinate-scatter conventions
\code{correct_landmarks(rule = "check_geometry")} audits still fails to hold,
by moving the deviant landmark(s) onto their group's shared reference
(an anatomical anchor, for the three two-point segments; the median of
every present point, for the two multi-point groups -- see Details).
Unlike \code{\link[=standardize_geometry]{standardize_geometry()}} (steps 1-3), this does change measured
distances/ratios for whichever landmark(s) it moves.
}
\details{
See \code{\link[=correct_geometry]{correct_geometry()}}'s Details for the full description of the five
checks corrected here (\code{perpendicular_seg_1_9_vs_axis},
\code{perpendicular_seg_3_4_vs_axis}, \code{perpendicular_seg_10_11_vs_axis},
\code{perpendicular_eye_vertical_vs_axis}, \code{axis_horizontal_parallel}), which
this function applies identically.

Because "vertical" and "horizontal" are evaluated directly on raw X/Y,
this function is only meaningful once the main axis is already
horizontal -- i.e. on the output of \code{\link[=standardize_geometry]{standardize_geometry()}} (or
\code{\link[=correct_geometry]{correct_geometry()}}, which performs both steps). Calling it on raw,
un-standardized coordinates will evaluate these checks against whatever
arbitrary orientation the photograph happened to have, producing
corrections that do not reflect the intended FISHMORPH conventions. A
specimen missing landmark 1 or 2 (needed to compute body length, Bl, the
denominator \code{tolerance_coord} is expressed against) is skipped, with a
warning.

As with \code{\link[=correct_geometry]{correct_geometry()}}, running this function twice on
already-corrected data is harmless (idempotent up to floating-point
precision): it only ever corrects a coordinate onto another point's
already-present value.

Splitting \code{\link[=correct_geometry]{correct_geometry()}} this way lets you inspect
\code{correct_landmarks(rule = "check_geometry")}'s audit, or a diagnostic
like a functional-space sensitivity comparison, before deciding whether
this active correction is appropriate for a given data set or clade --
e.g. a species with a genuinely atypical body plan (very elongate,
dorsoventrally flattened, oblique/ventral mouth) may legitimately
violate these generic conventions as a matter of true anatomy rather
than digitization noise, in which case applying only
\code{\link[=standardize_geometry]{standardize_geometry()}} (or raising \code{tolerance_coord} well above the
default here) may be preferable to this function's default, strict
correction for those specimens.
}
\examples{
fish <- load_t26_saudrune_landmarks()
fish_std <- standardize_geometry(fish) # steps 1-3 first, required
fish_corrected <- correct_geometry_conventions(fish_std)
attr(fish_corrected$coords, "correction_log")
# equivalent, in one call:
fish_corrected2 <- correct_geometry(fish)

}
\seealso{
\code{\link[=standardize_geometry]{standardize_geometry()}} (steps 1-3, which never change any
value), \code{\link[=correct_geometry]{correct_geometry()}} (the combined pipeline, unchanged and
still the recommended one-call route for existing workflows),
\code{\link[=correct_landmarks]{correct_landmarks()}}, \code{\link[=plot_fishmorph_points]{plot_fishmorph_points()}}
}
