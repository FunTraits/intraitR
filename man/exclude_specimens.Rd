% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exclude_specimens.R
\name{exclude_specimens}
\alias{exclude_specimens}
\title{Exclude known-bad specimens (e.g. mismeasured fish) from a landmark data set}
\usage{
exclude_specimens(landmarks, specimen, reason = NA_character_)
}
\arguments{
\item{landmarks}{An object of class \code{"intrait_landmarks"}, or a raw
\verb{p x k x n} landmark array. Not \code{"intrait_gpa"} (the output of
\code{\link[=gpa_fish]{gpa_fish()}}) -- see Details.}

\item{specimen}{Character vector of specimen identifiers (matching
\code{dimnames(landmarks$coords)[[3]]}, i.e. the row names used throughout
the package, such as \code{rownames(landmarks$metadata)}), or an integer
vector of positions, to remove. Every name must actually exist; unlike
a manual \code{dplyr::filter(Code != "...")}, a typo or formatting mismatch
(e.g. a leading zero, or \code{Site} vs \code{site}) errors immediately instead
of silently matching (and removing) nothing.}

\item{reason}{Optional character vector explaining why each specimen in
\code{specimen} is excluded (e.g. \code{"visibly mis-measured; landmarks 3-4 and 5-6 collapsed to the same point"}), recorded alongside the specimen
identifier for a full, reproducible audit trail (see Return). Either
length 1 (recycled for every specimen) or the same length as
\code{specimen}. Defaults to \code{NA}, i.e. unrecorded.}
}
\value{
An object of the same class as \code{landmarks}, with the specified
specimen(s) removed from \code{coords} (and from \code{scale}/\code{metadata}, if
present). Any \code{standardization_log}/\code{correction_log}/\code{corrected}/
\code{orientation_log} attribute already present on \code{coords} (from an
earlier \code{\link[=standardize_orientation]{standardize_orientation()}}/\code{\link[=standardize_geometry]{standardize_geometry()}}/
\code{\link[=correct_geometry]{correct_geometry()}}/\code{\link[=correct_geometry_conventions]{correct_geometry_conventions()}}/
\code{\link[=correct_landmarks]{correct_landmarks()}} call) is filtered the same way, so the audit
trail never refers to a specimen no longer in the data. A
\code{removed_specimens} element (\code{intrait_landmarks} input) or attribute
(raw array input) records every exclusion made so far as a
\code{data.frame} with columns \code{specimen}, \code{reason}; calling
\code{exclude_specimens()} again on an already-cleaned object accumulates
into the same record rather than replacing it, so the complete
history of exclusions stays attached to the data, matching the same
spirit as \code{\link[=gpa_fish]{gpa_fish()}}'s \code{remove_outliers}/\verb{$removed_outliers}.
}
\description{
Removes one or more specimens from an \code{"intrait_landmarks"} object (as
returned by \code{\link[=read_tps]{read_tps()}}, \code{\link[=read_landmarks_csv]{read_landmarks_csv()}},
\code{\link[=read_landmarks_xlsx]{read_landmarks_xlsx()}}, or \code{\link[=load_t26_saudrune_landmarks]{load_t26_saudrune_landmarks()}}) or a raw
\verb{p x k x n} landmark array, dropping them consistently from the
coordinate array \emph{and} from \verb{$scale}/\verb{$metadata} (if present), and
recording exactly which specimens were removed and why, so the
exclusion is reproducible and does not depend on remembering to repeat
a manual \code{dplyr::filter()} (or similar) every time the raw data is
reloaded.
}
\details{
Intended to be called right after loading raw landmark data (e.g.
immediately after \code{\link[=load_t26_saudrune_landmarks]{load_t26_saudrune_landmarks()}} or
\code{\link[=read_landmarks_xlsx]{read_landmarks_xlsx()}}), once a specimen has been confirmed -- e.g. via
\code{\link[=plot_fishmorph_points]{plot_fishmorph_points()}}, \code{\link[=correct_landmarks]{correct_landmarks()}} (\code{rule = "check_geometry"}), or the non-finite-ratio error from \code{\link[=trait_space]{trait_space()}}/
\code{\link[=fishmorph_ratios]{fishmorph_ratios()}} -- to be a genuine measurement/digitization error
rather than a real, if unusual, morphology; every downstream function
in the package (\code{\link[=fishmorph_segments]{fishmorph_segments()}}, \code{\link[=gpa_fish]{gpa_fish()}},
\code{\link[=correct_geometry]{correct_geometry()}}, ...) then simply never sees it, rather than
relying on filtering \code{segments}/\code{ratios}/\code{trait_space()} output after
the fact at every stage of the pipeline (easy to forget to repeat
consistently, and too late for any function -- like \code{\link[=standardize_geometry]{standardize_geometry()}}
or \code{\link[=gpa_fish]{gpa_fish()}} -- whose result for the \emph{other}, retained specimens can
itself depend on which specimens were included).

Not supported for \code{"intrait_gpa"} objects (the output of \code{\link[=gpa_fish]{gpa_fish()}}):
Procrustes alignment is computed jointly across every specimen supplied
to \code{\link[=gpa_fish]{gpa_fish()}}, so a mis-digitized specimen can distort the consensus
shape (and hence every other specimen's alignment to it); simply
deleting its row from an already-aligned \code{coords} array afterwards does
not undo that distortion. Call \code{exclude_specimens()} \emph{before}
\code{\link[=gpa_fish]{gpa_fish()}} instead (on the raw digitized landmarks), or use
\code{\link[=gpa_fish]{gpa_fish()}}'s own \code{remove_outliers = TRUE}, which re-runs
\code{\link[geomorph:gpagen]{geomorph::gpagen()}} on the cleaned sample and records the exclusion in
\verb{$removed_outliers}.
}
\examples{
fish <- load_t26_saudrune_landmarks()

# after visually confirming these two specimens are mismeasured (not
# just morphologically unusual), remove them right after loading, so
# every downstream step (fishmorph_segments(), gpa_fish(), ...) is
# computed without them:
fish_clean <- exclude_specimens(
  fish,
  specimen = c("T-26-0050_Operator_2", "T-26-0230-1_Operator_2"),
  reason = "landmarks 3-4 and/or 5-6 collapsed to the same point (zero-length Bd/Hd)"
)
fish_clean$removed_specimens # full record: which, and why

}
\seealso{
\code{\link[=gpa_fish]{gpa_fish()}} (\code{remove_outliers}, for exclusions decided \emph{after}
Procrustes alignment), \code{\link[=detect_outliers]{detect_outliers()}}, \code{\link[=correct_landmarks]{correct_landmarks()}}
(\code{rule = "check_geometry"}), \code{\link[=plot_fishmorph_points]{plot_fishmorph_points()}},
\code{\link[=load_t26_saudrune_landmarks]{load_t26_saudrune_landmarks()}}, \code{\link[=read_landmarks_xlsx]{read_landmarks_xlsx()}}
}
