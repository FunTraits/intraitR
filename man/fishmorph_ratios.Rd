% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fishmorph_ratios.R
\name{fishmorph_ratios}
\alias{fishmorph_ratios}
\title{Compute the nine FISHMORPH unitless ecomorphological ratios}
\usage{
fishmorph_ratios(
  segments,
  MBl = NULL,
  no_caudal_fin = FALSE,
  ventral_mouth = FALSE,
  no_pectoral_fin = FALSE,
  digits = 4
)
}
\arguments{
\item{segments}{A \code{data.frame} as returned by \code{\link[=fishmorph_segments]{fishmorph_segments()}}, or
any \code{data.frame} containing (at least) numeric columns \code{Bl}, \code{Bd},
\code{Hd}, \code{Eh}, \code{Mo}, \code{PFi}, \code{PFl}, \code{Ed}, \code{Jl}, \code{CPd}, \code{CFd}.}

\item{MBl}{Optional numeric vector of maximum body length (one value
per row of \code{segments}, same order), used only to add an \code{MBl} column
to the output; not used in any ratio computation.}

\item{no_caudal_fin}{Logical, length 1 or \code{nrow(segments)}. \code{TRUE} for
specimens with no visible caudal fin (e.g. Sternopygidae, Anguillidae,
Plotosidae), for which caudal peduncle throttling (\code{CPt}) is set to 1
(caudal fin depth assumed equal to caudal peduncle depth), following
Villéger et al. (2010). Defaults to \code{FALSE}.}

\item{ventral_mouth}{Logical, length 1 or \code{nrow(segments)}. \code{TRUE} for
algae-browsing species with the mouth positioned under the body
(e.g. Loricariidae, some Balitoridae such as \emph{Gastromyzon}), for which
oral gape position (\code{OGp}) and relative maxillary length (\code{RMl}) are
set to 0, following Villéger et al. (2010). Defaults to \code{FALSE}.}

\item{no_pectoral_fin}{Logical, length 1 or \code{nrow(segments)}. \code{TRUE} for
species without pectoral fins (e.g. Synbranchiformes, some
Anguilliformes), for which pectoral fin vertical position (\code{PFv}) is
set to 0, following Villéger et al. (2010). Defaults to \code{FALSE}.}

\item{digits}{Integer, number of decimal places to round ratios to.
Defaults to \code{4}.}
}
\value{
A \code{data.frame} (class \code{"intrait_fishmorph"}) with one row per
specimen, an \code{MBl} column if supplied, and columns \code{BEl}, \code{VEp},
\code{REs}, \code{OGp}, \code{RMl}, \code{BLs}, \code{PFv}, \code{PFs}, \code{CPt}, preceded by any
metadata columns carried over from \code{segments}.
}
\description{
Derives the nine dimensionless morphological ratios (traits) defined by
Brosse et al. (2021) for their FISHMORPH database, from the 11 linear
measurements produced by \code{\link[=fishmorph_segments]{fishmorph_segments()}}, and optionally
assembles the complete 10-trait FISHMORPH table by adding maximum body
length (\code{MBl}), a species-level, literature-derived size trait not
measured from the picture itself (e.g. taken from FishBase).
}
\details{
The nine ratios and their ecological interpretation, following Brosse
et al. (2021, their figure 1b), are: body elongation
(\code{BEl = Bl / Bd}, hydrodynamism), vertical eye position
(\code{VEp = Eh / Bd}, position of the fish and/or its prey in the water
column), relative eye size (\code{REs = Ed / Hd}, visual acuity), oral gape
position (\code{OGp = Mo / Bd}, feeding position in the water column),
relative maxillary length (\code{RMl = Jl / Hd}, mouth size and jaw
strength), body lateral shape (\code{BLs = Hd / Bd}, hydrodynamism and head
size), pectoral fin vertical position (\code{PFv = PFi / Bd}, pectoral fin
use for swimming), pectoral fin size (\code{PFs = PFl / Bl}, pectoral fin
use for swimming), and caudal peduncle throttling
(\code{CPt = CFd / CPd}, caudal propulsion efficiency through reduction of
drag).

Note that a fourth exception described by Villéger et al. (2010) — for
flatfishes (Pleuronectiformes), body depth (\code{Bd}) should be measured as
body \emph{width} rather than body depth, because the fish lies on one side
of its body in lateral-view pictures — cannot be corrected after the
fact and must instead be applied when digitizing landmarks 3-4
(see \code{\link[=fishmorph_segments]{fishmorph_segments()}}).
}
\examples{
fish <- load_t26_saudrune_landmarks()
segments <- fishmorph_segments(fish)
fishmorph_ratios(segments)

}
\references{
Brosse, S., Charpin, N., Su, G., Toussaint, A., Herrera-R, G. A.,
Tedesco, P. A., & Villéger, S. (2021). FISHMORPH: A global database on
morphological traits of freshwater fishes. Global Ecology and
Biogeography, 30(11), 2330-2336.

Villéger, S., Ramos Miranda, J., Flores Hernandez, D., & Mouillot, D.
(2010). Contrasting changes in taxonomic vs. functional diversity of
tropical fish communities after habitat degradation. Ecological
Applications, 20(6), 1512-1522.
}
\seealso{
\code{\link[=fishmorph_segments]{fishmorph_segments()}}, \code{\link[=trait_space]{trait_space()}}, \code{\link[=load_t26_saudrune_landmarks]{load_t26_saudrune_landmarks()}}
}
