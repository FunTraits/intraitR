% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fishmorph_ratios.R
\name{fishmorph_ratios}
\alias{fishmorph_ratios}
\title{Compute the nine FISHMORPH unitless ecomorphological ratios}
\usage{
fishmorph_ratios(
  segments,
  MBl = NULL,
  no_caudal_fin = FALSE,
  ventral_mouth = FALSE,
  no_pectoral_fin = FALSE,
  digits = 4,
  groups = NULL,
  na_action = c("keep", "omit", "impute_mean", "impute_group_mean", "missforest",
    "missforest_phylo"),
  missforest_ntree = 100,
  missforest_maxiter = 10,
  tree = NULL,
  missforest_phylo_k = 10,
  landmarks = NULL
)
}
\arguments{
\item{segments}{A \code{data.frame} as returned by \code{\link[=fishmorph_segments]{fishmorph_segments()}}, or
any \code{data.frame} containing (at least) numeric columns \code{Bl}, \code{Bd},
\code{Hd}, \code{Eh}, \code{Mo}, \code{PFi}, \code{PFl}, \code{Ed}, \code{Jl}, \code{CPd}, \code{CFd}.}

\item{MBl}{Optional numeric vector of maximum body length (one value
per row of \code{segments}, same order), used only to add an \code{MBl} column
to the output; not used in any ratio computation.}

\item{no_caudal_fin}{Logical, length 1 or \code{nrow(segments)}. \code{TRUE} for
specimens with no visible caudal fin (e.g. Sternopygidae, Anguillidae,
Plotosidae), for which caudal peduncle throttling (\code{CPt}) is set to 1
(caudal fin depth assumed equal to caudal peduncle depth), following
Villéger et al. (2010). Defaults to \code{FALSE}.}

\item{ventral_mouth}{Logical, length 1 or \code{nrow(segments)}. \code{TRUE} for
algae-browsing species with the mouth positioned under the body
(e.g. Loricariidae, some Balitoridae such as \emph{Gastromyzon}), for which
oral gape position (\code{OGp}) and relative maxillary length (\code{RMl}) are
set to 0, following Villéger et al. (2010). Defaults to \code{FALSE}.}

\item{no_pectoral_fin}{Logical, length 1 or \code{nrow(segments)}. \code{TRUE} for
species without pectoral fins (e.g. Synbranchiformes, some
Anguilliformes), for which pectoral fin vertical position (\code{PFv}) is
set to 0, following Villéger et al. (2010). Defaults to \code{FALSE}.}

\item{digits}{Integer, number of decimal places to round ratios to.
Defaults to \code{4}.}

\item{groups}{Optional factor (or character vector), one value per row
of \code{segments}, used only by \code{na_action = "impute_group_mean"} (and
optionally by \code{"missforest"}, as an auxiliary predictor). If \code{NULL}
and \code{segments} has a \code{species} column, it is used automatically (as
in \code{\link[=trait_space]{trait_space()}}).}

\item{na_action}{Character, how to handle missing values in the 9
computed ratio columns (e.g. because one of the two segments a ratio
divides was itself \code{NA}, whether from a missing landmark or from
\code{\link[=fishmorph_segments]{fishmorph_segments()}} having already left it \code{NA}): \code{"keep"}
(default) leaves \code{NA} in place, exactly as in previous package
versions; \code{"omit"} removes affected specimens and reports how many;
\code{"impute_mean"} replaces missing ratio values with the column mean;
\code{"impute_group_mean"} uses the within-group (e.g. within-species)
mean instead, falling back to the column mean, with a warning, for a
group entirely missing a ratio; \code{"missforest"} uses random-forest-
based iterative imputation (\code{missForest::missForest()});
\code{"missforest_phylo"} does the same but additionally augments the
predictor matrix with phylogenetic PCoA axes (see \code{\link[=phylo_pcoa]{phylo_pcoa()}},
\code{tree}) for the species in \code{groups}, falling back to plain
\code{"missforest"} (with a warning) if phylogenetic axes cannot be used.
Same convention, options, and messages as \code{\link[=trait_space]{trait_space()}}'s
\code{na_action} -- see there for details -- except that here imputation
operates on the derived \emph{ratios} directly; imputing at the landmark
level with \code{\link[=impute_landmarks]{impute_landmarks()}}, or at the \code{\link[=fishmorph_segments]{fishmorph_segments()}}
stage instead (before computing ratios), is usually preferable when
both are missing for the same specimen.}

\item{missforest_ntree, missforest_maxiter}{Number of trees per forest
and maximum number of iterations passed to \code{missForest::missForest()}
when \code{na_action} is \code{"missforest"}/\code{"missforest_phylo"}; ignored
otherwise. Default to \code{missForest}'s own defaults (\code{100} and \code{10}).}

\item{tree}{Used only by \code{na_action = "missforest_phylo"}: an object of
class \code{"phylo"}, or \code{NULL} (default) to use the bundled
\code{\link[=load_fishmorph_phylogeny]{load_fishmorph_phylogeny()}} tree.}

\item{missforest_phylo_k}{Used only by \code{na_action = "missforest_phylo"}:
maximum number of phylogenetic PCoA axes to add as predictors.
Defaults to \code{10}.}

\item{landmarks}{Optional: the same \code{"intrait_landmarks"} object or raw
\verb{p x k x n} array originally passed to \code{\link[=fishmorph_segments]{fishmorph_segments()}} to
produce \code{segments}. When supplied, rescues the nine ratios (not the
absolute segment values, nor \code{MBl}) for specimens whose scale bar
(landmarks 20-21) was missing or invalid -- i.e. whose row in
\code{segments} is entirely \code{NA}, \code{\link[=fishmorph_segments]{fishmorph_segments()}}'s signature for
this case. Because every ratio here divides two measurements from the
very same specimen, an unknown/missing scale factor cancels out
algebraically, so these ratios can be recomputed directly from the raw
pixel-space landmark distances, without ever needing a valid scale bar.
Matched to \code{segments} by row name/specimen id; only rescues rows that
are entirely \code{NA} (a specimen with just one missing anatomical
landmark, or a single \code{geometry_check}-flagged segment, is left
untouched, since mixing pixel-space and calibrated values for the same
specimen would defeat that quality control). Ignored, with no effect,
if \code{NULL} (default).}
}
\value{
A \code{data.frame} (class \code{"intrait_fishmorph"}) with one row per
specimen (fewer, if \code{na_action = "omit"} dropped any), an \code{MBl}
column if supplied, and columns \code{BEl}, \code{VEp}, \code{REs}, \code{OGp}, \code{RMl},
\code{BLs}, \code{PFv}, \code{PFs}, \code{CPt}, preceded by any metadata columns carried
over from \code{segments}.
}
\description{
Derives the nine dimensionless morphological ratios (traits) defined by
Brosse et al. (2021) for their FISHMORPH database, from the 11 linear
measurements produced by \code{\link[=fishmorph_segments]{fishmorph_segments()}}, and optionally
assembles the complete 10-trait FISHMORPH table by adding maximum body
length (\code{MBl}), a species-level, literature-derived size trait not
measured from the picture itself (e.g. taken from FishBase).
}
\details{
The nine ratios and their ecological interpretation, following Brosse
et al. (2021, their figure 1b), are: body elongation
(\code{BEl = Bl / Bd}, hydrodynamism), vertical eye position
(\code{VEp = Eh / Bd}, position of the fish and/or its prey in the water
column), relative eye size (\code{REs = Ed / Hd}, visual acuity), oral gape
position (\code{OGp = Mo / Bd}, feeding position in the water column),
relative maxillary length (\code{RMl = Jl / Hd}, mouth size and jaw
strength), body lateral shape (\code{BLs = Hd / Bd}, hydrodynamism and head
size), pectoral fin vertical position (\code{PFv = PFi / Bd}, pectoral fin
use for swimming), pectoral fin size (\code{PFs = PFl / Bl}, pectoral fin
use for swimming), and caudal peduncle throttling
(\code{CPt = CFd / CPd}, caudal propulsion efficiency through reduction of
drag).

Note that a fourth exception described by Villéger et al. (2010) — for
flatfishes (Pleuronectiformes), body depth (\code{Bd}) should be measured as
body \emph{width} rather than body depth, because the fish lies on one side
of its body in lateral-view pictures — cannot be corrected after the
fact and must instead be applied when digitizing landmarks 3-4
(see \code{\link[=fishmorph_segments]{fishmorph_segments()}}).

A missing or zero-length scale bar makes every one of
\code{\link[=fishmorph_segments]{fishmorph_segments()}}'s 11 measurements \code{NA} for that specimen (the
pixel-to-centimetre conversion factor is a single, per-specimen scalar
applied to all of them), which would normally also make all nine ratios
\code{NA}. Passing the original \code{landmarks} here recovers them anyway:
because a ratio is always (segment / segment) \emph{for that same specimen},
the shared, unknown scale factor cancels out of the division exactly,
so the ratio is identical whether computed from calibrated (cm) or raw
(pixel) segment values. This cannot recover the absolute segments
themselves (\code{Bl}, \code{Bd}, ...) or \code{MBl}, only the nine unitless ratios.
}
\examples{
fish <- load_t26_saudrune_landmarks()
segments <- fishmorph_segments(fish)
fishmorph_ratios(segments)

# impute missing ratios (na_action defaults to "keep") using the
# within-species mean instead of carrying NA forward:
fishmorph_ratios(segments, groups = segments$species, na_action = "impute_group_mean")

# rescue ratios (only) for specimens with a missing/invalid scale bar,
# directly from pixel-space landmark distances:
fishmorph_ratios(segments, landmarks = fish)

}
\references{
Brosse, S., Charpin, N., Su, G., Toussaint, A., Herrera-R, G. A.,
Tedesco, P. A., & Villéger, S. (2021). FISHMORPH: A global database on
morphological traits of freshwater fishes. Global Ecology and
Biogeography, 30(11), 2330-2336.

Villéger, S., Ramos Miranda, J., Flores Hernandez, D., & Mouillot, D.
(2010). Contrasting changes in taxonomic vs. functional diversity of
tropical fish communities after habitat degradation. Ecological
Applications, 20(6), 1512-1522.
}
\seealso{
\code{\link[=fishmorph_segments]{fishmorph_segments()}}, \code{\link[=trait_space]{trait_space()}},
\code{\link[=load_t26_saudrune_landmarks]{load_t26_saudrune_landmarks()}}, \code{\link[=impute_landmarks]{impute_landmarks()}}
}
