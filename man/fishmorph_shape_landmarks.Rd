% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fishmorph_shape_landmarks.R
\name{fishmorph_shape_landmarks}
\alias{fishmorph_shape_landmarks}
\title{Extract complete-case body-shape landmarks from a FISHMORPH configuration}
\usage{
fishmorph_shape_landmarks(landmarks, species = NULL, drop_incomplete = TRUE)
}
\arguments{
\item{landmarks}{An object of class \code{"intrait_landmarks"} with at least
the 21 FISHMORPH landmarks (see \code{\link[=fishmorph_segments]{fishmorph_segments()}}).}

\item{species}{Optional character vector (or factor), one value per
specimen, used only to additionally drop specimens with a missing
(\code{NA}) value -- e.g. an unresolved identification -- so that a
downstream grouped analysis (\code{\link[=morpho_space]{morpho_space()}},
\code{\link[=intraspecific_variability]{intraspecific_variability()}}, \code{\link[=detect_outliers]{detect_outliers()}}) never encounters
an \code{NA} group. Defaults to \code{landmarks$metadata$species} if present,
otherwise \code{NULL} (no filtering on species).}

\item{drop_incomplete}{Logical, drop specimens missing one or more of
landmarks 1-19 (plus 22, if present). Defaults to \code{TRUE}; set to
\code{FALSE} to keep every specimen and inspect missingness yourself (e.g.
via \code{\link[=impute_landmarks]{impute_landmarks()}} instead of dropping).}
}
\value{
An object of the same class as \code{landmarks}, with landmarks 20-21
removed and, if \code{drop_incomplete}, incomplete/unidentified specimens
removed from \code{coords}, \code{metadata}, and \code{scale} alike.
}
\description{
Prepares a FISHMORPH-scheme \code{"intrait_landmarks"} object (21+ points; see
\code{\link[=fishmorph_segments]{fishmorph_segments()}}) for Generalised Procrustes Analysis
(\code{\link[=gpa_fish]{gpa_fish()}}) or any other function that expects a single, complete set
of homologous shape landmarks. Two adjustments are needed before such a
configuration can be treated as pure shape data, and this function
applies both at once: the embedded scale bar (landmarks 20-21) is
dropped, since it is a fixed calibration segment rather than a body
landmark and mixing it into a Procrustes superimposition (which treats
every point as part of the shape) would distort the alignment; and, by
default, any specimen missing one or more of the remaining landmarks (or
with a missing/unresolved species identification, if \code{species} is
supplied) is dropped, since GPA requires complete configurations.
}
\examples{
fish <- load_t26_saudrune_landmarks()
fish_shape <- fishmorph_shape_landmarks(fish)
dim(fish$coords)
dim(fish_shape$coords)
gpa <- gpa_fish(fish_shape)
gpa

}
\seealso{
\code{\link[=gpa_fish]{gpa_fish()}}, \code{\link[=fishmorph_segments]{fishmorph_segments()}}, \code{\link[=impute_landmarks]{impute_landmarks()}},
\code{\link[=load_t26_saudrune_landmarks]{load_t26_saudrune_landmarks()}}
}
