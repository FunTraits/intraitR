% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/group_colors.R
\name{group_colors}
\alias{group_colors}
\title{Look up the group/species colours used by the ordination plot methods}
\usage{
group_colors(x)
}
\arguments{
\item{x}{Either an object with a \verb{$groups} element (e.g. one returned by
\code{\link[=morpho_space]{morpho_space()}}/\code{\link[=trait_space]{trait_space()}}), or a factor/character vector of
group labels directly (e.g. \code{fish$metadata$species}), one per
observation -- duplicates are fine, only the distinct values matter.}
}
\value{
A \code{data.frame} with one row per distinct group (in the same
order used by the plot methods' own legend), and columns \code{group}
(character) and \code{color} (hex character).
}
\description{
Returns the exact colour \code{\link[=plot.intrait_morphospace]{plot.intrait_morphospace()}}/
\code{\link[=plot.intrait_traitspace]{plot.intrait_traitspace()}} draw (or would draw) for each group, in the
same order as their own legend -- so a shared legend built separately
(e.g. one common legend below several panels laid out with
\code{par(mfrow = ...)}/\code{layout()}, each plotted with \code{legend = FALSE}) is
guaranteed to match every panel's actual colours, without reimplementing
or guessing at the underlying colour assignment.
}
\details{
Looking a group up here does not itself add it to the session-level
colour cache in a way that is any different from plotting it directly:
either way, a group not yet seen this session is assigned the next
unused colour and keeps it for the rest of the session (see
\code{\link[=reset_group_colors]{reset_group_colors()}}). Calling \code{group_colors()} \emph{before} plotting is
therefore entirely safe and produces the same colours the subsequent
plot calls will use.
}
\examples{
fish <- simulate_fish_landmarks(n_per_species = 5, n_replicates = 1)
gpa <- gpa_fish(fish)
ms <- morpho_space(gpa, groups = fish$metadata$species)
group_colors(ms)

# or directly from a label vector:
group_colors(fish$metadata$species)
}
\seealso{
\code{\link[=reset_group_colors]{reset_group_colors()}}, \code{\link[=plot.intrait_morphospace]{plot.intrait_morphospace()}},
\code{\link[=plot.intrait_traitspace]{plot.intrait_traitspace()}}
}
