% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute_traits.R
\name{impute_traits}
\alias{impute_traits}
\title{Impute missing trait values, and say which cells were invented}
\usage{
impute_traits(
  traits,
  method = c("missforest_phylo", "missforest", "impute_group_mean", "impute_mean",
    "omit", "keep"),
  groups = NULL,
  species = NULL,
  tree = NULL,
  phylo_axes = NULL,
  missforest_phylo_k = 10,
  missforest_ntree = 100,
  missforest_maxiter = 10
)
}
\arguments{
\item{traits}{A data.frame or matrix of numeric traits, one row per
specimen. Non-numeric columns are dropped, with a warning.}

\item{method}{Imputation method: \code{"impute_mean"}, \code{"impute_group_mean"},
\code{"missforest"} or \code{"missforest_phylo"}. \code{"omit"} drops the incomplete rows
instead of filling them, and \code{"keep"} returns the table untouched -- both
accepted so that a caller can pass the user's choice through unchanged.}

\item{groups}{Grouping factor, one value per row: the key of
\code{"impute_group_mean"} and a categorical predictor for the forests. \code{NULL}
uses \code{species} when it is given.}

\item{species}{Species name per row, used only to look up the phylogenetic
axes of \code{"missforest_phylo"}.}

\item{tree, phylo_axes, missforest_phylo_k}{Phylogeny for
\code{"missforest_phylo"}; see \code{\link[=trait_space]{trait_space()}}.}

\item{missforest_ntree, missforest_maxiter}{Passed to
\code{\link[missForest:missForest]{missForest::missForest()}}.}
}
\value{
A list of class \code{"intrait_imputation"}:
\describe{
\item{\code{traits}}{the filled data.frame, in the order given}
\item{\code{imputed}}{a logical matrix of the same shape, \code{TRUE} where a
value was invented}
\item{\code{keep}}{logical vector, \code{FALSE} for rows dropped by \code{"omit"}}
\item{\code{method}}{the method applied}
\item{\code{n_missing},\code{n_imputed}}{counts before and after}
}
}
\description{
The imputation that \code{\link[=trait_space]{trait_space()}}, \code{\link[=fishmorph_ratios]{fishmorph_ratios()}} and
\code{\link[=fishmorph_segments]{fishmorph_segments()}} perform internally, exposed on its own so that a
filled trait table can be \strong{kept} rather than recomputed inside every
ordination -- and, above all, so that the cells that were filled can travel
with it.
}
\section{Why the mask matters more than the values}{

An imputed value is indistinguishable from a measured one once it is in a
table: it has the same type, the same magnitude, and it makes every
completeness check pass. The \code{imputed} matrix returned here is the only
thing that keeps the two apart afterwards, and it is what lets a downstream
analysis weight, exclude or simply report them. A function that returned the
filled matrix alone would be handing back a table that has quietly stopped
being a record of measurements.
}

\section{Choosing a method}{

\code{"impute_group_mean"} replaces a missing value with the mean of its species.
It is transparent and reproducible, and it CONTRACTS INTRASPECIFIC
VARIABILITY -- every imputed individual is placed exactly at its species'
centre -- which is a problem when intraspecific variability is the quantity
being measured. \code{"missforest"} predicts each trait from the others by random
forest and preserves more of the spread; \code{"missforest_phylo"} adds
phylogenetic PCoA axes as predictors (see \code{\link[=phylo_pcoa]{phylo_pcoa()}}) and falls back to
plain \code{"missforest"}, with a message, when no usable tree is available.
Both report an out-of-bag NRMSE, which is the honest measure of how much the
filled values can be trusted.
}

\examples{
set.seed(1)
d <- data.frame(a = rnorm(20), b = rnorm(20), c = rnorm(20))
d$a[c(2, 7)] <- NA
im <- impute_traits(d, method = "impute_mean")
sum(im$imputed)
}
\seealso{
\code{\link[=trait_space]{trait_space()}}, \code{\link[=fishmorph_ratios]{fishmorph_ratios()}}, \code{\link[=impute_landmarks]{impute_landmarks()}}
}
