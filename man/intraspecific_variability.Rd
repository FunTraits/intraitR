% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/intraspecific_variability.R
\name{intraspecific_variability}
\alias{intraspecific_variability}
\title{Quantify intraspecific morphological variability}
\usage{
intraspecific_variability(gpa = NULL, groups, traits = NULL, iter = 999)
}
\arguments{
\item{gpa}{Optional object of class \code{"intrait_gpa"} (from
\code{\link[=gpa_fish]{gpa_fish()}}). Required for the shape-based disparity analysis.}

\item{groups}{A factor or character vector giving the grouping variable
(e.g. species or population), of the same length and order as the
specimens in \code{gpa} and/or \code{traits}.}

\item{traits}{Optional \code{data.frame} of linear traits or ratios (e.g.
from \code{\link[=linear_distances]{linear_distances()}} or \code{\link[=morpho_ratios]{morpho_ratios()}}), one row per specimen
in the same order as \code{groups}. Non-numeric columns are ignored.}

\item{iter}{Integer, number of permutations for the disparity test.
Defaults to \code{999}.}
}
\value{
An object of class \code{"intrait_variability"}, a list optionally
containing:
\describe{
\item{shape_disparity}{the \code{geomorph} \code{"morphol.disparity"} object
(Procrustes variance per group, with pairwise permutation
p-values), when \code{gpa} is supplied.}
\item{trait_cv}{a tidy \code{data.frame} with columns \code{group}, \code{trait},
\code{n}, \code{mean}, \code{sd}, \code{cv_percent}, when \code{traits} is supplied.}
}
}
\description{
Combines two complementary approaches to intraspecific variability
commonly used in fish ecomorphology: (i) shape-based morphological
disparity per group, via a permutation test on Procrustes variance
(\code{\link[geomorph:morphol.disparity]{geomorph::morphol.disparity()}}), and (ii) classical coefficients of
variation (CV\%) of linear traits or ratios per group.
}
\details{
Procrustes variance (mean squared Procrustes distance to the group mean
shape) is a standard, unit-free measure of shape disparity and is
preferred over CV for shape data because Procrustes coordinates do not
have an interpretable scale on their own axes. Coefficients of variation
remain informative and widely reported for univariate, biologically
interpretable traits (e.g. body depth ratio) and are provided alongside
shape disparity for that reason.
}
\examples{
fish <- simulate_fish_landmarks(n_per_species = 10, n_replicates = 1)
gpa <- gpa_fish(fish)
distances <- list(SL = c(1, 7), BD = c(3, 10))
ratios <- morpho_ratios(fish, distances, norm_by = "SL")
\donttest{
iv <- intraspecific_variability(
  gpa = gpa, groups = fish$metadata$species,
  traits = ratios[, "BD_ratio", drop = FALSE], iter = 99
)
iv
}

}
\references{
Zelditch ML, Swiderski DL, Sheets HD (2012). Geometric Morphometrics for
Biologists: A Primer (2nd ed). Academic Press.
}
\seealso{
\code{\link[=gpa_fish]{gpa_fish()}}, \code{\link[=morpho_ratios]{morpho_ratios()}}, \code{\link[=measurement_error]{measurement_error()}}
}
