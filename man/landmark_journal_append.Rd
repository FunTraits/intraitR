% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/landmark_journal.R
\name{landmark_journal_append}
\alias{landmark_journal_append}
\title{Append one record (one digitization) to a landmark journal}
\usage{
landmark_journal_append(
  journal,
  row_key,
  coords,
  points,
  status = NULL,
  specimen = NA,
  individual = NA,
  replicate = 1L,
  photo_file = NA,
  mode = NA,
  target_sheet = NA,
  img_w = NA,
  img_h = NA,
  quality = NA,
  ruler_mm = NA,
  mm_per_px = NA
)
}
\arguments{
\item{journal}{A handle from \code{\link[=landmark_journal_open]{landmark_journal_open()}}.}

\item{row_key}{Deduplication key: the saved identifier (an individual, or
\code{"<individual>_<operator>_rep<N>"} for a repeated digitization).}

\item{coords}{Two-column matrix (X, Y) indexed by landmark number.}

\item{points}{Landmark numbers to record.}

\item{status}{Optional named character vector (names = point numbers) of
per-point statuses; see the \code{status} values in the package \code{NEWS}. Points
without a finite coordinate are recorded as \code{"na"} whatever is passed.}

\item{specimen, individual, replicate, photo_file, mode, target_sheet}{Record-level
metadata, recycled over the points.}

\item{img_w, img_h, quality, ruler_mm, mm_per_px}{Further record-level metadata.}
}
\value{
The \code{record_id} written (invisibly), or \code{NULL} if there was nothing
to write.
}
\description{
A "record" is one press of the app's \emph{Save} button: one row per landmark,
all sharing the same \code{record_id}. Writing is a plain \code{cat(append = TRUE)} of
a text block built beforehand -- the existing file is never re-read nor
rewritten, so an interruption can only truncate the last line, which
\code{\link[=landmark_journal_read]{landmark_journal_read()}} then discards.
}
\examples{
d <- file.path(tempdir(), "journal_demo2")
jr <- landmark_journal_open(d, operator = "AT")
P <- cbind(X = c(10, 60), Y = c(20, 22))
landmark_journal_append(jr, row_key = "fish_01", coords = P, points = 1:2,
                        specimen = "fish_01", individual = "fish_01")
nrow(landmark_journal_read(d))
unlink(d, recursive = TRUE)

}
\seealso{
\code{\link[=landmark_journal_open]{landmark_journal_open()}}, \code{\link[=landmark_journal_read]{landmark_journal_read()}}
}
