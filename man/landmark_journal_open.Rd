% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/landmark_journal.R
\name{landmark_journal_open}
\alias{landmark_journal_open}
\title{Open a session landmark journal (append-only)}
\usage{
landmark_journal_open(
  journal_dir,
  operator = NULL,
  app_version = NA_character_
)
}
\arguments{
\item{journal_dir}{Directory holding the journals. Created if absent.}

\item{operator}{Operator identifier, traced in every row and in the session
file name. \code{NULL} (default) uses the system user.}

\item{app_version}{Version of the digitizing tool, traced in every row: it is
what makes it possible to know, in two years, with which geometry a given
specimen was digitized.}
}
\value{
An object of class \code{"intrait_journal"}: a handle to pass to
\code{\link[=landmark_journal_append]{landmark_journal_append()}}, carrying the journal \code{path}, the \code{operator}
and the \code{session_id}.
}
\description{
Creates \code{journal_dir} if needed and a TSV file specific to this session. The
file is only ever written to by appending: it is never re-read nor rewritten
while the session runs, and becomes immutable the moment the session ends.
This is the capture layer behind \code{\link[=digitize_landmarks]{digitize_landmarks()}}, and the reason a
crashed session costs at most the specimen being digitized rather than the
whole data set.
}
\examples{
d <- file.path(tempdir(), "journal_demo")
jr <- landmark_journal_open(d, operator = "AT")
basename(jr$path)
unlink(d, recursive = TRUE)

}
\seealso{
\code{\link[=landmark_journal_append]{landmark_journal_append()}}, \code{\link[=landmark_journal_read]{landmark_journal_read()}},
\code{\link[=consolidate_landmarks]{consolidate_landmarks()}}, \code{\link[=digitize_landmarks]{digitize_landmarks()}}
}
