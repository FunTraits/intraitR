% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/landmark_journal.R
\name{landmark_journal_read}
\alias{landmark_journal_read}
\title{Read and concatenate every journal in a directory}
\usage{
landmark_journal_read(journal_dir)
}
\arguments{
\item{journal_dir}{Journal directory, or a vector of directories.}
}
\value{
A LONG \code{data.frame}, one row per point and per record, with the
columns listed in \code{intraitR:::.INTRAITR_JOURNAL_COLS}. Coordinates,
\code{replicate}, \code{quality}, \code{img_w}, \code{img_h}, \code{ruler_mm} and \code{mm_per_px} are
returned numeric; everything else is character.
}
\description{
Tolerant by construction: a last line truncated by a crash is dropped
(mandatory columns missing), and a journal written by an earlier version
(fewer columns) is filled with \code{NA}. Two workstations' journals merge by
plain concatenation.
}
\examples{
d <- file.path(tempdir(), "journal_demo3")
jr <- landmark_journal_open(d, operator = "AT")
landmark_journal_append(jr, "fish_01", cbind(c(10, 60), c(20, 22)), 1:2,
                        specimen = "fish_01", individual = "fish_01")
str(landmark_journal_read(d)[c("specimen", "landmark", "x", "y", "status")])
unlink(d, recursive = TRUE)

}
\seealso{
\code{\link[=consolidate_landmarks]{consolidate_landmarks()}}, \code{\link[=landmark_journal_open]{landmark_journal_open()}}
}
