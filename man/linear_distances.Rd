% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/linear_distances.R
\name{linear_distances}
\alias{linear_distances}
\title{Compute inter-landmark linear distances}
\usage{
linear_distances(landmarks, pairs, scale = NULL)
}
\arguments{
\item{landmarks}{An object of class \code{"intrait_landmarks"} or
\code{"intrait_gpa"}, or a raw \verb{p x k x n} landmark array. Note: distances
are normally computed on \strong{raw} (pre-Procrustes) coordinates, since
Generalised Procrustes Analysis removes size information; passing an
\code{"intrait_gpa"} object will return distances in the arbitrary
unit-centroid-size scale of the Procrustes fit.}

\item{pairs}{A (optionally named) list of length-2 integer vectors, each
giving the indices of two landmarks whose distance should be computed.
List names become the trait names in the output (e.g.
\code{list(SL = c(1, 2), BD = c(3, 4))}).}

\item{scale}{Optional numeric vector of scale factors (real-world units
per coordinate unit), one per specimen and named to match specimen
identifiers. If \code{NULL} and \code{landmarks} is an \code{"intrait_landmarks"}
object with a non-missing \code{scale} element, that scale is used
automatically.}
}
\value{
A \code{data.frame} with one row per specimen (row names = specimen
identifiers) and one column per entry in \code{pairs}.
}
\description{
Computes Euclidean distances between user-defined pairs of landmarks
(e.g. a truss network, Strauss & Bookstein, 1982) on raw, un-aligned
landmark coordinates, optionally converted to real-world units using a
digitization scale.
}
\examples{
# real T-26 Saudrune data; landmark indices follow the FISHMORPH scheme
# (see ?fishmorph_segments), so these pairs are anatomically meaningful.
# `fish$scale` is not set, so distances stay in raw digitization units
# here; use fishmorph_segments() instead for measurements already
# converted to centimetres via the embedded scale bar (points 20-21):
fish <- load_t26_saudrune_landmarks()
pairs <- list(SL = c(1, 2), BD = c(3, 4), HD = c(5, 6))
head(linear_distances(fish, pairs))

}
\seealso{
\code{\link[=morpho_ratios]{morpho_ratios()}}, \code{\link[=read_tps]{read_tps()}}
}
