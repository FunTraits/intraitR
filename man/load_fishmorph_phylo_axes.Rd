% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_fishmorph_phylo_axes.R
\name{load_fishmorph_phylo_axes}
\alias{load_fishmorph_phylo_axes}
\title{Precomputed phylogenetic PCoA axes for the FISHMORPH species pool}
\usage{
load_fishmorph_phylo_axes(file = NULL, k = NULL, refresh = FALSE)
}
\arguments{
\item{file}{Optional path to an alternative axis table: either an \code{.rds}
holding a data frame, or a whitespace-separated text file with species as
row names and one column per axis. \code{NULL} (default) uses the bundled file.}

\item{k}{Number of axes to return (the first \code{k} columns). \code{NULL} returns all.}

\item{refresh}{Force a re-read instead of using the session cache.}
}
\value{
A data frame with a \code{species} column (canonical \code{Genus_species}
spelling) followed by the axis columns, named \code{phylo_1}, \code{phylo_2}, ...
}
\description{
Loads \code{pcoaPhylogenyFish.rds}, the \strong{precomputed} principal-coordinate axes
of the bundled fish phylogeny for the 8,970 FISHMORPH species (10 axes,
ordered by decreasing eigenvalue). These are the axes used by every
\code{"missforest_phylo"} option in the package: \code{\link[=trait_space]{trait_space()}},
\code{\link[=fishmorph_segments]{fishmorph_segments()}}, \code{\link[=fishmorph_ratios]{fishmorph_ratios()}} and \code{\link[=impute_landmarks]{impute_landmarks()}}.
}
\section{Why precomputed}{

The alternative, \code{\link[=phylo_pcoa]{phylo_pcoa()}}, eigendecomposes the patristic distance matrix
of the tree. That matrix is \emph{n by n}: for 8,970 species it holds roughly 80
million entries, and the decomposition is cubic in \emph{n}. Recomputing it on
every imputation is both slow and, more importantly, \strong{not comparable across
calls} -- the axes depend on which subset of species happens to be present in
the data at hand, so two analyses run on different subsets end up in different
phylogenetic coordinate systems and their results cannot be compared.

Reading a fixed table instead makes the axes a \emph{property of the phylogeny}
rather than of the current dataset. Every imputation, whatever the species
subset, then lives in one and the same phylogenetic space.
}

\section{File format}{

The bundled table is a compressed \code{.rds} (about 540 kB, against 1.8 MB for the
whitespace-separated text it was produced from). Both formats are accepted and
dispatched on the file extension, so \code{file} may point at either. To regenerate
the \code{.rds} from a text source:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{txt <- read.table("pcoaPhylogenyFish.txt", header = TRUE)
ax  <- data.frame(species = gsub("[ ._]+", "_", rownames(txt)), txt,
                  row.names = NULL)
names(ax)[-1] <- paste0("phylo_", seq_len(ncol(txt)))
saveRDS(ax, "pcoaPhylogenyFish.rds", compress = "xz")
}\if{html}{\out{</div>}}
}

\examples{
ax <- load_fishmorph_phylo_axes(k = 3)
dim(ax)
head(ax, 3)

}
\seealso{
\code{\link[=phylo_pcoa]{phylo_pcoa()}} to recompute axes from an arbitrary tree,
\code{\link[=load_fishmorph_phylogeny]{load_fishmorph_phylogeny()}} for the tree itself, \code{\link[=trait_space]{trait_space()}} and
\code{\link[=impute_landmarks]{impute_landmarks()}} for the imputation methods that consume these axes.
}
