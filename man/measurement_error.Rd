% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/measurement_error.R
\name{measurement_error}
\alias{measurement_error}
\title{Estimate measurement error from replicated digitization}
\usage{
measurement_error(
  x,
  individual = NULL,
  method = c("anova", "procrustes"),
  iter = 999
)
}
\arguments{
\item{x}{For \code{method = "anova"}: either a numeric matrix/data.frame with
one row per individual and one column per replicate measurement, or a
long-format \code{data.frame} with an \code{individual} grouping column and a
\code{value} column. For \code{method = "procrustes"}: an object of class
\code{"intrait_gpa"} built from replicate-digitized specimens (i.e. each
individual appears multiple times in the sample, once per
digitization replicate).}

\item{individual}{For long-format univariate input, the name of the
column identifying individuals. For \code{method = "procrustes"}, a factor
(or character vector) of the same length as the number of specimens
in \code{x}, giving the individual identity of each replicate. Required
for \code{method = "procrustes"}.}

\item{method}{Character, one of \code{"anova"} (default) or \code{"procrustes"}.}

\item{iter}{Integer, number of permutations for \code{method = "procrustes"}.
Defaults to \code{999}.}
}
\value{
An object of class \code{"intrait_measurement_error"}.
For \code{method = "anova"}, a list with \code{anova_table} (a one-way ANOVA of
trait value on individual identity), \code{percent_measurement_error}
(\verb{\%ME}, the proportion of total variance attributable to measurement
error), and \code{repeatability} (the intraclass correlation coefficient,
\code{R}). For \code{method = "procrustes"}, a list with \code{procD_table}, the
Procrustes ANOVA table testing whether shape variance among
individuals exceeds variance among replicates within individuals.
}
\description{
Quantifies measurement error and repeatability from repeated
measurements (replicate digitizations of the same specimen), for either
univariate linear traits/ratios (ANOVA-based percent measurement error
and repeatability, Bailey & Byrnes, 1990; Yezerinac et al., 1992) or
landmark shape data (Procrustes ANOVA, Fruciano, 2016). Assessing
measurement error is a necessary step before interpreting any
biological pattern of intraspecific variability, since it partitions
observed variance into a biological and a technical (digitization)
component.
}
\details{
For \code{method = "anova"}, with among-individual and residual (replicate)
mean squares \eqn{MS_a} and \eqn{MS_e} from a one-way ANOVA, and \code{n}
replicates per individual:
\deqn{\%ME = \frac{MS_e}{MS_e + MS_a} \times 100}
\deqn{R = \frac{MS_a - MS_e}{MS_a + (n - 1) MS_e}}
Low \verb{\%ME} (and high \code{R}, close to 1) indicate that measurement error is
small relative to genuine among-individual variation, and that
subsequent analyses of intraspecific variability
(\code{\link[=intraspecific_variability]{intraspecific_variability()}}) are unlikely to be confounded by
digitization noise.
}
\examples{
set.seed(1)
replicate_data <- data.frame(
  r1 = rnorm(10, 50, 5), r2 = rnorm(10, 50, 5), r3 = rnorm(10, 50, 5)
)
rownames(replicate_data) <- paste0("ind", 1:10)
measurement_error(replicate_data, method = "anova")

}
\references{
Bailey RC, Byrnes J (1990). A new, old method for assessing measurement
error in both univariate and multivariate morphometric studies.
Systematic Zoology, 39(2), 124-130.

Yezerinac SM, Lougheed SC, Handford P (1992). Measurement error and
morphometric studies: statistical power and observational error.
Systematic Biology, 41(4), 471-482.

Fruciano C (2016). Measurement error in geometric morphometrics.
Development Genes and Evolution, 226(3), 139-158.
}
\seealso{
\code{\link[=gpa_fish]{gpa_fish()}}, \code{\link[=intraspecific_variability]{intraspecific_variability()}}
}
