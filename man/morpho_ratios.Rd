% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/morpho_ratios.R
\name{morpho_ratios}
\alias{morpho_ratios}
\title{Compute classical fish morphometric ratios}
\usage{
morpho_ratios(landmarks, distances, norm_by, scale = NULL, digits = 4)
}
\arguments{
\item{landmarks}{An object of class \code{"intrait_landmarks"} or a raw
\verb{p x k x n} landmark array (raw, un-aligned coordinates; see
\code{\link[=linear_distances]{linear_distances()}}).}

\item{distances}{A named list of length-2 landmark index vectors,
passed to \code{\link[=linear_distances]{linear_distances()}} (e.g.
\code{list(SL = c(1, 2), BD = c(3, 4), ED = c(13, 14))} for FISHMORPH-scheme
landmarks; see \code{\link[=fishmorph_segments]{fishmorph_segments()}}). Must include the normalising
distance named in \code{norm_by}.}

\item{norm_by}{Character. Name of the entry in \code{distances} used as the
denominator for all other distances (typically standard length,
\code{"SL"}).}

\item{scale}{Optional named numeric vector of scale factors, as in
\code{\link[=linear_distances]{linear_distances()}}.}

\item{digits}{Integer, number of decimal places to round ratios to.
Defaults to \code{4}.}
}
\value{
A \code{data.frame} with one row per specimen, columns
\verb{<name>_ratio} for every entry in \code{distances} other than \code{norm_by},
and any metadata columns carried over from \code{landmarks} (if present).
}
\description{
Derives size-corrected morphological ratios from raw landmark
coordinates by dividing a set of inter-landmark distances by a chosen
normalising distance (typically standard length). Ratios of this kind
are widely used in fish ecomorphology to relate body shape to
ecological function (e.g. body depth ratio as a proxy for swimming
performance, eye diameter ratio for visual ecology; Winemiller, 1991;
Pease et al., 2012).
}
\details{
Ratios are dimensionless and therefore comparable across specimens
irrespective of the digitization scale, provided the same unit is used
for numerator and denominator; as a result, \code{scale} only needs to be
supplied if all distances are to also be reported at their original
(unscaled) magnitude via \code{\link[=linear_distances]{linear_distances()}} beforehand, or if
specimens were digitized at different magnifications, in which case the
scale must be supplied to keep ratios strictly comparable.
}
\examples{
# real T-26 Saudrune data; landmark indices follow the FISHMORPH scheme
# (see ?fishmorph_segments)
fish <- load_t26_saudrune_landmarks()
distances <- list(SL = c(1, 2), BD = c(3, 4), ED = c(13, 14))
head(morpho_ratios(fish, distances, norm_by = "SL"))

}
\references{
Winemiller KO (1991). Ecomorphological diversification in lowland
freshwater fish assemblages from five biotic regions. Ecological
Monographs, 61(4), 343-365.

Pease AA, Gonzalez-Diaz AA, Rodiles-Hernandez R, Winemiller KO (2012).
Functional diversity and trait-environment relationships of stream fish
assemblages in a large tropical catchment. Freshwater Biology, 57(5),
1060-1075.
}
\seealso{
\code{\link[=linear_distances]{linear_distances()}}, \code{\link[=summary_traits]{summary_traits()}}
}
