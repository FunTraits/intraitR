% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/operator_disagreement.R
\name{operator_disagreement}
\alias{operator_disagreement}
\alias{print.intrait_operator_disagreement}
\alias{plot.intrait_operator_disagreement}
\title{Flag individuals whose landmark configuration disagrees across operators,
and attribute the disagreement to a specific operator}
\usage{
operator_disagreement(
  landmarks,
  individual = NULL,
  operator = NULL,
  species = NULL,
  normalization = c("centroid_size", "landmarks", "standard_length"),
  ref_landmarks = c(1, 2),
  exclude_landmarks = NULL,
  reference_operator = NULL,
  threshold = 3,
  min_operators = 2,
  digits = 4
)

\method{print}{intrait_operator_disagreement}(x, ...)

\method{plot}{intrait_operator_disagreement}(x, type = c("individual", "operator", "landmark"), ...)
}
\arguments{
\item{landmarks}{An object of class \code{"intrait_landmarks"} (from
\code{\link[=read_tps]{read_tps()}}, \code{\link[=read_landmarks_csv]{read_landmarks_csv()}}, \code{\link[=read_mlmorph_landmarks]{read_mlmorph_landmarks()}}, or
\code{\link[=load_t26_saudrune_landmarks]{load_t26_saudrune_landmarks()}}) in which each physical individual has
been digitized by two or more operators (one or more configurations per
operator; see \code{individual}/\code{operator}).}

\item{individual}{A factor or character vector with one entry per
specimen/row in \code{landmarks} (length \code{dim(landmarks$coords)[3]}), giving
the identity of the physical individual each digitization belongs to.
Defaults to \code{landmarks$metadata$individual} if present.}

\item{operator}{A factor or character vector of the same length, giving
the operator who produced each digitization. Defaults to
\code{landmarks$metadata$operator} if present (as produced by
\code{\link[=load_t26_saudrune_landmarks]{load_t26_saudrune_landmarks()}} with \code{source = "operators"}). When an
operator digitized the same individual more than once, that operator's
replicate configurations are first averaged (landmark-wise mean,
ignoring \code{NA}) into a single per-operator configuration, so every
operator contributes exactly one configuration per individual and no
operator is implicitly up-weighted by having digitized an individual
more often.}

\item{species}{A factor or character vector of the same length, giving
species identity, used only to annotate the output. Defaults to
\code{landmarks$metadata$species} if present.}

\item{normalization}{Character, one of \code{"centroid_size"} (default),
\code{"landmarks"}, or \code{"standard_length"}, giving the per-individual
reference distance by which raw landmark displacements are divided so
that the disagreement index is a size-free percentage, comparable
across individuals of very different body size (as in
\code{\link[=digitization_error]{digitization_error()}}). \code{"centroid_size"} uses each individual's own
mean landmark-configuration centroid size (averaged over its operators;
Bookstein, 1991), the self-contained default that requires no landmark
choice and matches the centre-and-scale view drawn by
\code{\link[=plot_fishmorph_shapes]{plot_fishmorph_shapes()}} (\code{align = TRUE}); \code{"landmarks"} uses the
inter-landmark distance between \code{ref_landmarks} (averaged over the
individual's operators); \code{"standard_length"} uses the individual's mean
\code{standard_length_mm} from \code{landmarks$metadata}. Unlike
\code{\link[=digitization_error]{digitization_error()}}, the reference is always computed \strong{per
individual} (not per species), since the quantity of interest here is
how much the operators of \emph{one} fish disagree relative to \emph{that} fish's
size.}

\item{ref_landmarks}{Integer vector of length 2, the two landmarks whose
distance is the size reference when \code{normalization = "landmarks"}.
Defaults to \code{c(1, 2)}. Ignored otherwise.}

\item{exclude_landmarks}{Optional integer vector of landmark indices to
exclude from the disagreement calculation entirely -- most importantly
the embedded scale-bar calibration points (landmarks 20-21) of the
FISHMORPH scheme (\code{\link[=fishmorph_segments]{fishmorph_segments()}}), which encode a fixed
real-world distance rather than a body landmark and so are not a
homologous point whose across-operator scatter is meaningful. Defaults
to \code{NULL} (all landmarks used); pass \code{c(20, 21)} for FISHMORPH-scheme
data (see Examples). Excluded landmarks may still be used in
\code{ref_landmarks}.}

\item{reference_operator}{Optional character scalar naming one operator
(a value of \code{operator}) to treat as a gold-standard reference against
which the others are measured, e.g. an expert digitizer. When supplied,
each other operator's deviation for an individual is its distance to the
reference operator's configuration of that individual, and the
responsible operator is the non-reference operator that deviates most
from it. This makes operator attribution identifiable even for
individuals digitized by only two operators (see Details). Defaults to
\code{NULL}: attribution is then made against a leave-one-out consensus of
the other operators, which is only identifiable when an individual has
at least three operators (again, see Details). The disagreement
\emph{magnitude} (\code{disagreement_pct}) is a symmetric, reference-free quantity
and is unaffected by \code{reference_operator}.}

\item{threshold}{Numeric, the number of median absolute deviations (MAD)
above the median disagreement index beyond which an individual is
flagged as at-risk. Defaults to \code{3}, the same robust rule (and default)
as \code{\link[=detect_outliers]{detect_outliers()}} and the within-group screen underlying
\code{\link[=species_sensitivity]{species_sensitivity()}}. The median and MAD are used, rather than the
mean and SD, because they are themselves resistant to the discordant
individuals being screened for.}

\item{min_operators}{Integer, the minimum number of distinct operators an
individual must have to be included. Defaults to \code{2} (the smallest
number for which any disagreement can be defined). Individuals digitized
by fewer operators are dropped, with a message.}

\item{digits}{Integer, number of decimal places to round percentages to.
Defaults to \code{4}.}

\item{x}{An object of class \code{"intrait_operator_disagreement"}.}

\item{...}{Currently unused (\code{print}) or passed to \code{\link[graphics:plot]{graphics::plot()}} /
\code{\link[graphics:barplot]{graphics::barplot()}} (\code{plot}).}

\item{type}{Character, one of \code{"individual"} (default), \code{"operator"}, or
\code{"landmark"}, selecting which view \code{\link[=plot.intrait_operator_disagreement]{plot.intrait_operator_disagreement()}}
draws: a ranked dot plot of every individual's disagreement index with
at-risk individuals highlighted and the threshold marked (as in
\code{\link[=detect_outliers]{detect_outliers()}}); a bar plot of mean deviation per operator; or a bar
plot of mean across-operator spread per landmark.}
}
\value{
An object of class \code{"intrait_operator_disagreement"}, a list with:
\describe{
\item{\code{by_individual}}{\code{data.frame}, one row per retained individual,
ordered by decreasing disagreement, with columns \code{individual},
\code{species}, \code{n_operators}, \code{operators} (comma-separated labels),
\code{disagreement_pct} (the index: the mean across landmarks of the
root-mean-square across-operator displacement from the per-landmark
consensus, as a percentage of the reference distance),
\code{max_landmark_pct} (the single most discordant landmark's value),
\code{at_risk} (logical, \code{disagreement_pct} exceeds the robust
threshold), \code{responsible_operator} (the attributed operator, or \code{NA}
when not identifiable -- see Details), \code{responsible_deviation_pct},
and \code{attribution_margin_pct} (how far ahead of the next operator the
attributed one is; small values mean a low-confidence attribution).}
\item{\code{by_operator}}{\code{data.frame}, one row per operator, with the
number of individuals it digitized, its mean and SD deviation across
them, and how often (count and percentage, among identifiable
individuals) it was the responsible operator -- the systematic view
that also resolves, at the population level, the two-operator
individuals whose culprit is not identifiable one at a time.}
\item{\code{by_landmark}}{\code{data.frame}, one row per landmark, giving the
mean/median/SD across-operator spread at that landmark over all
individuals, ordered by decreasing mean spread -- which anatomical
points the operators disagree on most.}
\item{\code{landmark_operator}}{long \code{data.frame}, one row per
individual x operator x landmark, of normalised displacements from
the per-landmark consensus, for drill-down.}
\item{\code{threshold_value}}{the numeric \code{disagreement_pct} cut-off implied
by \code{threshold}.}
\item{\code{threshold}, \code{normalization}, \code{reference_operator},
\code{excluded_landmarks}}{the settings used.}
}
Has dedicated \code{print()} and \code{plot()} methods.

Invisibly returns \code{x}.

Invisibly returns \code{x}.
}
\description{
Screens a landmark data set in which the \emph{same} physical individuals were
each digitized once (or a few times) by \emph{several} independent operators
-- as produced by \code{\link[=load_t26_saudrune_landmarks]{load_t26_saudrune_landmarks()}} with
\code{source = "operators"} -- and returns, for every individual, a single
inter-operator disagreement index together with an automatic "at-risk"
flag and, where identifiable, the operator responsible for the
disagreement. It is the population-level, one-number-per-individual
companion to the by-eye, one-individual-at-a-time overlay of
\code{\link[=plot_fishmorph_shapes]{plot_fishmorph_shapes()}} (\code{operator = TRUE}): rather than paging through
every fish to spot the ones whose operators drew visibly different shapes,
\code{operator_disagreement()} ranks all individuals by how much their
operators disagree, flags the unusually discordant ones, and names the
operator whose placement is the outlier.
}
\details{
For a given individual and landmark, let the (single, per-operator)
positions be \eqn{(x_o, y_o)} for operators \eqn{o = 1, \dots, K}, with
consensus (mean across operators) \eqn{(\bar{x}, \bar{y})}. Operator
\eqn{o}'s displacement is \eqn{d_o = \sqrt{(x_o - \bar{x})^2 + (y_o -
\bar{y})^2}}, and the landmark's across-operator \emph{spread} is the
root-mean-square \eqn{\sqrt{\frac{1}{K}\sum_o d_o^2}}. The individual's
disagreement index is the mean of that spread over all (included)
landmarks, divided by the individual's reference distance and expressed as
a percentage (see \code{normalization}). This is the inter-operator analogue of
the intra-operator, repeated-digitization bias of \code{\link[=digitization_error]{digitization_error()}}:
there the replicated configurations come from \emph{one} operator digitizing
the same specimen repeatedly (quantifying repeatability); here they come
from \emph{different} operators digitizing it once each (quantifying
inter-operator disagreement, the "several independent operators" case that
\code{\link[=digitization_error]{digitization_error()}} explicitly does not cover; Klingenberg & McIntyre,
1998). Because the calculation is landmark by landmark, it should be
applied only to homologous, independently placed body landmarks: fixed
calibration points such as the FISHMORPH scale bar (landmarks 20-21)
should be dropped via \code{exclude_landmarks} (see Examples).

\strong{Operator attribution.} Detecting \emph{that} the operators of a fish
disagree is symmetric; attributing the disagreement to \emph{one} of them is
not always possible. With \code{reference_operator = NULL} (the default), the
responsible operator is the one lying furthest from the leave-one-out
consensus of the \emph{other} operators (a majority-vote logic robust to the
full consensus being dragged toward the outlier). This is well defined
only when an individual has at least three operators: for exactly two
operators the two leave-one-out deviations are equal by construction
(each operator's "consensus of the others" is simply the other operator),
so neither can be singled out and \code{responsible_operator} is \code{NA} -- the
disagreement magnitude is still reported, and the \code{by_operator} table
still reveals which operator is \emph{systematically} discordant across the
whole data set even when no single two-operator fish can be adjudicated.
Supplying \code{reference_operator} (e.g. a trusted expert) breaks this
symmetry: every other operator is then measured against that reference, so
attribution becomes identifiable for two-operator individuals as well.
\code{responsible_operator} is reported for every identifiable individual, but
is only \emph{meaningful} for those flagged \code{at_risk}; for the rest,
\code{attribution_margin_pct} is typically small (the nominally most-deviant
operator is barely ahead), a signal not to over-interpret it.

Like \code{\link[=detect_outliers]{detect_outliers()}}, this is a fast screening tool, not a formal
test: a genuinely and correctly digitized but naturally unusual fish, or
an individual for which one landmark is legitimately ambiguous, may be
flagged. Always inspect flagged individuals visually (e.g. with
\code{\link[=plot_fishmorph_shapes]{plot_fishmorph_shapes()}}, \code{operator = TRUE}, exactly the per-individual
overlay this function summarises) before acting on the flag. For a
rotation-invariant, formal Procrustes-ANOVA treatment of the same
replicated-digitization design, see \code{\link[=measurement_error]{measurement_error()}}
(\code{method = "procrustes"}).
}
\examples{
# T-26 Saudrune: every fish digitized once by each of several operators.
fish <- load_t26_saudrune_landmarks(source = "operators")

# FISHMORPH scheme: drop the scale bar (landmarks 20-21) before screening.
od <- operator_disagreement(fish, exclude_landmarks = c(20, 21))
od

# the ranked, flagged table of individuals and their culprit operator:
head(od$by_individual)

# which operator is systematically the most discordant?
od$by_operator

# visually confirm a flagged individual (the overlay this summarises):
risky <- od$by_individual$individual[od$by_individual$at_risk]
if (length(risky) > 0) {
  plot_fishmorph_shapes(fish, individuals = risky[1],
                        operator = TRUE, alpha = 0.6)
}

# treat Operator_1 as an expert reference so two-operator fish can also
# have their culprit named:
od_ref <- operator_disagreement(
  fish, exclude_landmarks = c(20, 21), reference_operator = "Operator_1"
)

}
\references{
Bookstein FL (1991). Morphometric Tools for Landmark Data: Geometry and
Biology. Cambridge University Press.

Klingenberg CP, McIntyre GS (1998). Geometric morphometrics of
developmental instability: analyzing patterns of fluctuating asymmetry
with Procrustes methods. Evolution, 52(5), 1363-1375.
}
\seealso{
\code{\link[=digitization_error]{digitization_error()}} (intra-operator repeatability from
repeated digitization), \code{\link[=measurement_error]{measurement_error()}} (Procrustes-ANOVA
measurement error), \code{\link[=detect_outliers]{detect_outliers()}} (Procrustes-distance outlier
screen), \code{\link[=plot_fishmorph_shapes]{plot_fishmorph_shapes()}} (the per-individual, operator-coloured
overlay this function summarises), \code{\link[=load_t26_saudrune_landmarks]{load_t26_saudrune_landmarks()}}
}
