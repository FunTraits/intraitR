% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phylo_pcoa.R
\name{phylo_pcoa}
\alias{phylo_pcoa}
\alias{print.intrait_phylopcoa}
\title{Phylogenetic Principal Coordinates Analysis}
\usage{
phylo_pcoa(
  tree,
  species = NULL,
  k = NULL,
  correction = c("none", "cailliez", "lingoes"),
  ultrametric = TRUE,
  ultrametric_method = c("nnls", "extend")
)

\method{print}{intrait_phylopcoa}(x, ...)
}
\arguments{
\item{tree}{An object of class \code{"phylo"} (e.g. from \code{ape::read.tree()},
\code{ape::read.nexus()}, or the bundled \code{\link[=load_fishmorph_phylogeny]{load_fishmorph_phylogeny()}}), with
tip labels matching the species names used elsewhere in your analysis
(e.g. \code{fish$metadata$species}, with spaces, underscores, and dots
treated as interchangeable -- see Details).}

\item{species}{Optional character vector of species to retain (typically
your actual species pool, e.g. \code{levels(factor(fish$metadata$species))}).
Species not found among \code{tree$tip.label} are dropped with a warning;
defaults to all of \code{tree$tip.label}.}

\item{k}{Integer, the number of phylogenetic axes to keep. Defaults to
all axes with a (corrected, if \code{correction != "none"}) positive
eigenvalue.}

\item{correction}{Character, \code{"none"} (default), \code{"cailliez"}, or
\code{"lingoes"} -- passed to \code{ape::pcoa()}'s \code{correction} argument.
Patristic/cophenetic distances are not guaranteed to be perfectly
Euclidean, which can produce a handful of small negative eigenvalues in
the decomposition; \code{"cailliez"} or \code{"lingoes"} add a constant to the
distances to remove them (see \code{?ape::pcoa}). Legendre & Anderson (1999)
found the Cailliez correction can inflate Type I error in downstream
permutation tests, so treat \code{"lingoes"} as the more conservative choice
if a correction is needed for such a test; \code{"none"} simply drops the
negative-eigenvalue axes (the \code{ape::pcoa()} default).}

\item{ultrametric}{Logical, coerce \code{tree} to be ultrametric (constant
root-to-tip distance) before computing distances, using
\code{phytools::force.ultrametric()}, if it is not already (to tolerance;
see \code{ape::is.ultrametric()}). Defaults to \code{TRUE}, matching a
time-calibrated phylogeny's own assumption; set to \code{FALSE} if \code{tree}'s
branch lengths are not meant to represent a strict molecular clock
(e.g. a phylogram in substitutions/site) and cophenetic distance is
still an appropriate summary for your purposes.}

\item{ultrametric_method}{Character, \code{"nnls"} (default) or \code{"extend"},
passed to \code{phytools::force.ultrametric()}'s \code{method} argument when
\code{ultrametric = TRUE} and \code{tree} is not already (numerically)
ultrametric; see \code{?phytools::force.ultrametric} -- this is a numerical
adjustment for representing the tree, not a re-estimation of
divergence times, and is reported with a \code{message()} when triggered.}

\item{x}{An object of class \code{"intrait_phylopcoa"}, as returned by
\code{\link[=phylo_pcoa]{phylo_pcoa()}}.}

\item{...}{Currently unused.}
}
\value{
An object of class \code{"intrait_phylopcoa"}, a list with elements
\code{traits} (a \code{data.frame}, one row per species, with a \code{species} column
and \code{k} numeric columns \code{PCoA1}, \code{PCoA2}, ... -- ready to pass as
\code{traits} to \code{\link[=trait_space]{trait_space()}}, which auto-detects \code{groups} from a
\code{species} column), \code{var_explained} (percent variance explained by each
retained axis), \code{k}, \code{correction}, \code{tree} (the pruned, and possibly
ultrametric-coerced, tree actually used), and \code{dropped_species} (any
\code{species} entries not found in \code{tree$tip.label}).

Invisibly returns \code{x}.
}
\description{
Derives quantitative "phylogenetic trait" axes from a phylogenetic tree,
by Principal Coordinates Analysis (PCoA) of the tree's patristic
(cophenetic) distances among species. The resulting axes summarise a
species' overall phylogenetic position as a small set of numeric
variables, in a \code{species}-plus-axes \code{data.frame} deliberately shaped so
it can be passed directly as the \code{traits} argument of \code{\link[=trait_space]{trait_space()}} --
letting you build a "phylogenetic space" with exactly the same ordination
machinery used for morphological trait spaces elsewhere in this package,
and then compare functional and phylogenetic diversity loss (e.g. with
\code{\link[=bootstrap_functional_space]{bootstrap_functional_space()}} or \code{\link[=species_sensitivity]{species_sensitivity()}}) using the same
statistics on both.
}
\details{
Species names are matched against \code{tree$tip.label} after collapsing any
run of spaces, underscores, and/or dots to a single underscore in both
(the various separator conventions used by different tip-label/species
sources -- e.g. \code{"Barbus_barbus"}, \code{"Barbus.barbus"}, as in the bundled
\code{\link[=load_fishmorph_phylogeny]{load_fishmorph_phylogeny()}} tree), so \code{species = "Barbus barbus"}
matches a tip labelled \code{"Barbus.barbus"} without requiring the caller to
reformat names first. At least 3 matched species are required for a
meaningful ordination.

This function deliberately only covers the generic, reusable step of
turning a tree plus a species list into ordination axes -- it does not
attempt taxonomic name resolution, tree sourcing, or trait/occurrence
data assembly, all of which are specific to wherever your tree and
species list come from and are out of scope for this package.
}
\examples{
\donttest{
if (requireNamespace("ape", quietly = TRUE)) {
  set.seed(1)
  tree <- ape::rcoal(8, tip.label = paste0("sp_", 1:8))
  pp <- phylo_pcoa(tree, k = 3)
  pp

  # use directly as `traits` in trait_space(): groups auto-detected from
  # the `species` column, exactly like a morphological trait matrix
  ts_phylo <- trait_space(pp$traits, na_action = "fail", log_transform = FALSE)
  plot(ts_phylo)
}
}

}
\references{
Cailliez, F. (1983). The analytical solution of the additive constant
problem. Psychometrika, 48, 305-308. \doi{10.1007/BF02294026}

Lingoes, J. C. (1971). Some boundary conditions for a monotone analysis
of symmetric matrices. Psychometrika, 36, 195-203.
\doi{10.1007/BF02291398}

Legendre, P., & Anderson, M. J. (1999). Distance-based redundancy
analysis: testing multispecies responses in multifactorial ecological
experiments. Ecological Monographs, 69(1), 1-24.
\doi{10.1890/0012-9615(1999)069[0001:DBRATM]2.0.CO;2}
}
\seealso{
\code{\link[=trait_space]{trait_space()}}, \code{\link[=bootstrap_functional_space]{bootstrap_functional_space()}},
\code{\link[=species_sensitivity]{species_sensitivity()}}
}
