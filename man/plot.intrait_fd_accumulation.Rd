% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fd_accumulation.R
\name{plot.intrait_fd_accumulation}
\alias{plot.intrait_fd_accumulation}
\title{Plot functional-diversity rarefaction curves}
\usage{
\method{plot}{intrait_fd_accumulation}(x, indices = NULL, band = TRUE, extrapolate = TRUE, ...)
}
\arguments{
\item{x}{An object of class \code{"intrait_fd_accumulation"}.}

\item{indices}{Optional character vector selecting which indices to draw.}

\item{band}{Logical, draw the resampling band. Defaults to \code{TRUE}.}

\item{extrapolate}{Logical, extend the fitted accumulation curve to \verb{n*}.
Defaults to \code{TRUE}.}

\item{...}{Passed to \code{\link[graphics:plot]{graphics::plot()}}.}
}
\value{
Invisibly returns \code{x}.
}
\description{
One panel per index: the resampling mean against sampling effort (with the
quantile band as a shaded envelope), the stabilisation effort \verb{n*} as a
vertical dashed line, and -- for the accumulation index -- the fitted
asymptote (dotted) with its dashed extrapolation, or -- for convergence
indices -- a dotted reference line at the full-effort value.
}
