% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/itv_index.R
\name{plot.intrait_itv}
\alias{plot.intrait_itv}
\title{Plot the interspecific/intraspecific variance breakdown}
\usage{
\method{plot}{intrait_itv}(x, trait_labels = "auto", sort = TRUE, legend_position = "bottom", ...)
}
\arguments{
\item{x}{An object of class \code{"intrait_itv"}, from \code{\link[=itv_index]{itv_index()}}.}

\item{trait_labels}{How to label each trait's bar. \code{"auto"} (default)
expands any trait code recognised as one of the nine FISHMORPH ratios
(see \code{\link[=fishmorph_ratios]{fishmorph_ratios()}}) to its full descriptive name, e.g. \code{"RMl"}
becomes \code{"Relative maxillary length (RMl)"}; codes not recognised
(i.e. any other kind of trait table) are left as-is. \code{NULL} always
uses the raw trait/column names unchanged. Alternatively, a named
character vector (\code{names} matching \code{x$per_trait$trait}, values the
display label to use) for full control over arbitrary trait sets.}

\item{sort}{Logical, sort bars by overall \%ITV (ascending internally, so
the highest ends up at the top of the figure) rather than plotting
traits in their original column order. Defaults to \code{TRUE}.}

\item{legend_position}{One of \code{"bottom"} (default: a horizontal legend
drawn just below the x-axis title) or a standard \code{\link[graphics:legend]{graphics::legend()}}
position keyword (e.g. \code{"topright"}) to draw it inside the plot box
instead.}

\item{...}{Further arguments passed to \code{\link[graphics:barplot]{graphics::barplot()}}.}
}
\value{
Invisibly returns \code{x}.
}
\description{
Horizontal stacked bar chart, one bar per trait, ranked by intraspecific
variability (ITV): the trait with the highest \%ITV at the top, the most
strongly species-differentiated trait at the bottom. The left margin is
sized automatically to fit whatever trait labels end up being used
(short codes or full descriptive names), so long labels are not cut off.
}
