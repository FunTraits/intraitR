% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/itv_accumulation.R
\name{plot.intrait_itv_accumulation}
\alias{plot.intrait_itv_accumulation}
\title{Plot intraspecific-variability rarefaction curves}
\usage{
\method{plot}{intrait_itv_accumulation}(x, series = NULL, band = TRUE, extrapolate = TRUE, xmax = NULL, ...)
}
\arguments{
\item{x}{An object of class \code{"intrait_itv_accumulation"}.}

\item{series}{Optional character vector selecting which trait series
(panels) to draw; defaults to all.}

\item{band}{Logical, draw the resampling quantile band. Defaults to
\code{TRUE}.}

\item{extrapolate}{Logical, for accumulation metrics extend the fitted
curve beyond the observed range up to \verb{n*} (or \code{xmax}) and draw the
fitted asymptote. Ignored for convergence framing. Defaults to \code{TRUE}.}

\item{xmax}{Optional numeric, the largest sample size to extrapolate to.
\code{NULL} (default) uses the largest finite \verb{n*} in the panel, capped at
ten times the observed maximum so a single very slowly saturating
series cannot squash the observed range; the fitted asymptote is always
drawn regardless, so the target remains visible even if the curve is
cut off before reaching it.}

\item{...}{Further arguments passed to \code{\link[graphics:plot]{graphics::plot()}}.}
}
\value{
Invisibly returns \code{x}.
}
\description{
One panel per trait series (arranged with \code{\link[graphics:par]{graphics::par()}} \code{mfrow}),
each showing the rarefied mean against sub-sample size for every group,
with the resampling quantile band as a shaded envelope and the estimated
stabilisation sample size \verb{n*} as a vertical dashed line. For
\emph{convergence} framing a horizontal reference line marks the full-sample
value. For \emph{accumulation} framing the observed portion is drawn solid and,
when \code{extrapolate = TRUE}, the fitted saturating model is extended in a
dashed line beyond the observed range up towards its estimated asymptote
(drawn as a horizontal dotted line), so the plateau the curve is heading
for is visible even when it lies well beyond the individuals actually
sampled -- the interpolation/extrapolation style of a rarefaction curve.
}
