% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trait_space.R
\name{plot.intrait_traitspace}
\alias{plot.intrait_traitspace}
\title{Plot a functional trait space}
\usage{
\method{plot}{intrait_traitspace}(
  x,
  style = c("spider", "hull", "density", "none"),
  ellipse_level = 0.95,
  density_level = 0.95,
  legend = !is.null(x$groups),
  legend_position = "outside",
  legend_title = "Group",
  legend_italic = FALSE,
  abbreviate_species = FALSE,
  ...
)
}
\arguments{
\item{x}{An object of class \code{"intrait_traitspace"}, from
\code{\link[=trait_space]{trait_space()}}.}

\item{style}{Character, one of \code{"spider"} (default), \code{"hull"},
\code{"density"}, or \code{"none"}, controlling how groups are displayed; see
the Details section of \code{\link[=plot.intrait_morphospace]{plot.intrait_morphospace()}}. Ignored if
\code{x$groups} is \code{NULL}. Also named in the plot's title (e.g. \code{"Trait space (spider)"}), so the display style used is always legible from
the figure itself, not just from the call that produced it; pass
\verb{main = } via \code{...} to override with a custom title instead.}

\item{ellipse_level}{Coverage probability of the per-group dispersion
ellipse drawn when \code{style = "spider"}, under a bivariate-normal
approximation. Defaults to \code{0.95}.}

\item{density_level}{Coverage probability of the per-group
kernel-density contour drawn when \code{style = "density"} (see Details of
\code{\link[=plot.intrait_morphospace]{plot.intrait_morphospace()}}); groups with fewer than 5 points are
silently skipped. Defaults to \code{0.95}.}

\item{legend}{Logical, draw a legend of group colors. Defaults to
\code{TRUE} when \code{x$groups} is available.}

\item{legend_position}{One of \code{"outside"} (default: drawn in the
margin, just outside the plot box, so it never overlaps the data) or
a standard \code{\link[graphics:legend]{graphics::legend()}} position keyword (e.g. \code{"topright"})
to draw it inside the plot box instead.}

\item{legend_title}{Character, the legend's title. Defaults to
\code{"Group"}; set to \code{"Species"} when \code{x$groups} represents species
identity (as it does, e.g., throughout \code{demo(pipeline_T26_saudrune)}).}

\item{legend_italic}{Logical, italicise the legend labels (standard
typographic convention for taxonomic names). Defaults to \code{FALSE}.}

\item{abbreviate_species}{Logical, abbreviate \code{"Genus species"} legend
labels to \code{"G. species"} (e.g. \code{"Barbatula barbatula"} becomes
\code{"B. barbatula"}); labels that are not a clean two-part binomial are
left unchanged. Only affects the legend text. Defaults to \code{FALSE}.}

\item{...}{Further arguments passed to \code{\link[graphics:plot]{graphics::plot()}}.}
}
\value{
Invisibly returns \code{x}.
}
\description{
Plot a functional trait space
}
\examples{
\donttest{
fish <- load_t26_saudrune_landmarks()
segments <- fishmorph_segments(fish)
ratios <- fishmorph_ratios(segments)
ts <- trait_space(ratios, groups = fish$metadata$species, na_action = "omit")
# species-flavoured legend: titled "Species", italic, abbreviated binomials
plot(ts, legend_title = "Species", legend_italic = TRUE, abbreviate_species = TRUE)
}
}
