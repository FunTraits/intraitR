% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_correlation_circle.R
\name{plot_correlation_circle}
\alias{plot_correlation_circle}
\title{Plot the correlation circle of a functional trait space}
\usage{
plot_correlation_circle(
  x,
  inner_circle = TRUE,
  cex_labels = 0.8,
  arrow_col = "firebrick3",
  label_col = "black",
  circle_col = "grey40",
  label_offset = 1.15,
  ...
)
}
\arguments{
\item{x}{An object of class \code{"intrait_traitspace"}, as returned by
\code{\link[=trait_space]{trait_space()}}.}

\item{inner_circle}{Logical, draw a dashed inner circle at radius
\code{sqrt(0.5)}, the conventional threshold above which a trait is
considered well represented by the two plotted axes (i.e. more than
half of that trait's variance is captured by them jointly). Defaults
to \code{TRUE}.}

\item{cex_labels}{Character expansion for trait labels. Defaults to
\code{0.8}.}

\item{arrow_col, label_col, circle_col}{Colours for the arrows, trait
labels, and circle(s), respectively.}

\item{label_offset}{Numeric, how far outside each arrow tip its label
is placed, as a multiple of the arrow's own length from the origin
(e.g. \code{1.15} places a label 15\% beyond the tip). Defaults to \code{1.15}.}

\item{...}{Further arguments passed to \code{\link[graphics:plot]{graphics::plot()}}.}
}
\value{
Invisibly returns a matrix of trait-axis correlations (one row
per trait actually used in \code{x}, one column per plotted axis) -- the
values represented by the arrows.
}
\description{
Draws the classical "correlation circle" (variable factor map) of a
\code{\link[=trait_space]{trait_space()}} ordination: each trait is shown as an arrow from the
origin to its Pearson correlation with the two plotted ordination axes,
inside a unit circle, the standard way of reading which traits drive
each axis and how well a trait is represented by the two-axis subspace
actually plotted (e.g. Legendre & Legendre, 2012, sec. 9.1.5).
}
\details{
Unlike a plot of raw PCA loadings (\code{x$loadings}), which are unit-norm
\emph{within} each axis and so are not directly comparable in length across
traits or informative about how well a trait is captured by the two
plotted axes jointly, the coordinates plotted here are the actual
Pearson correlation between each trait (as included in the ordination,
i.e. after any \code{log_transform}/\code{scale}/imputation/outlier removal --
see \code{x$X}) and the ordination scores on the two plotted axes (\code{x$scores}).
Because a Pearson correlation is always in \verb{[-1, 1]}, every arrow tip
necessarily falls on or inside the unit circle: a tip near the circle
means that trait is well summarised by the two-axis plane shown, while
a short arrow means most of that trait's variance lies on other,
unplotted axes (or, for \code{method = "pcoa"}, was not linearly captured by
this ordination at all). This computation does not depend on
\code{method} (\code{"pca"} or \code{"pcoa"}), or on whether \code{scale = TRUE} was used
when building \code{x}, since a Pearson correlation is itself scale-invariant.

The plot itself is drawn without a surrounding box: tick marks and
values (\code{-1} to \code{1}) are instead placed directly on the horizontal
(\code{y = 0}) and vertical (\code{x = 0}) reference lines through the origin,
and each line is labelled with its axis name only (e.g. \code{"PC1"}), in a
small italic font, just outside the unit circle and centred on the
line itself -- the standard presentation of a correlation circle in
the literature (e.g. \code{ade4::s.corcircle()}). The vertical axis's label
is itself drawn rotated (reading bottom to top), as for a conventional
\code{ylab}.
}
\examples{
fish <- load_t26_saudrune_landmarks()
segments <- fishmorph_segments(fish)
ratios <- fishmorph_ratios(segments)
ts <- trait_space(ratios, groups = fish$metadata$species, na_action = "omit")
\donttest{
plot_correlation_circle(ts)
}

}
\references{
Legendre, P., & Legendre, L. (2012). Numerical Ecology (3rd English
ed). Elsevier.
}
\seealso{
\code{\link[=trait_space]{trait_space()}}
}
