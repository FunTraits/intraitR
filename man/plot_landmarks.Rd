% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_landmarks.R
\name{plot_landmarks}
\alias{plot_landmarks}
\title{Plot a single landmark configuration}
\usage{
plot_landmarks(
  landmarks,
  specimen = 1,
  labels = TRUE,
  background_image = NULL,
  flip_y = TRUE,
  ...
)
}
\arguments{
\item{landmarks}{An object of class \code{"intrait_landmarks"} or
\code{"intrait_gpa"}, or a raw \verb{p x k x n} array. Must be two-dimensional.}

\item{specimen}{Integer index or character specimen identifier of the
configuration to plot. Defaults to \code{1}.}

\item{labels}{Logical, label landmarks with their index. Defaults to
\code{TRUE}.}

\item{background_image}{Optional path to a \code{.jpg}/\code{.jpeg} or \code{.png}
photograph of the specimen, drawn as a background layer beneath the
landmarks (e.g. to visually check whether a landmark was placed off
the body outline). Only meaningful for raw, un-aligned digitized
coordinates (a warning is issued if \code{landmarks} is an
\code{"intrait_gpa"} object, since Procrustes-aligned coordinates will not
line up with the original photograph). Requires the (Suggested)
\code{jpeg} package for \code{.jpg}/\code{.jpeg} files, or \code{png} for \code{.png} files.
Defaults to \code{NULL} (no background).}

\item{flip_y}{Logical, flip \code{background_image} vertically before
plotting. Image files are conventionally stored with row 1 at the
top, while digitized landmark coordinates (as read by
\code{\link[=read_tps]{read_tps()}}/\code{\link[geomorph:digitize2d]{geomorph::digitize2d()}}) place the origin at the
bottom-left; the default \code{TRUE} flips the image so it lines up with
the landmarks without needing to pre-flip the file itself. Ignored
when \code{background_image} is \code{NULL}.}

\item{...}{Further arguments passed to \code{\link[graphics:plot]{graphics::plot()}}.}
}
\value{
Invisibly returns the \verb{p x 2} matrix of coordinates plotted.
}
\description{
Produces a simple two-dimensional scatterplot of one specimen's
landmark configuration (raw or Procrustes-aligned), with landmarks
optionally numbered, for quality control of digitization and
configuration checking.
}
\details{
This is a deliberately generic viewer: it makes no assumption about
landmark count or anatomical scheme, and works equally well on raw,
Procrustes-aligned (\code{"intrait_gpa"}), or simulated configurations (e.g.
the \code{n_landmarks}-only shapes from \code{\link[=simulate_fish_landmarks]{simulate_fish_landmarks()}}) -- the
natural companion to the package's scheme-agnostic functions
(\code{\link[=gpa_fish]{gpa_fish()}}, \code{\link[=detect_outliers]{detect_outliers()}}, \code{\link[=correct_allometry]{correct_allometry()}},
\code{\link[=intraspecific_variability]{intraspecific_variability()}}, \code{\link[=morpho_space]{morpho_space()}}). For data digitized
following the FISHMORPH scheme specifically (Brosse et al. 2021, at
least 21 points), \code{\link[=plot_fishmorph_points]{plot_fishmorph_points()}} is usually more
informative -- it colours the 11 measurement segments, draws the body
outline/eye/scale bar, and highlights imputed/corrected/geometry-
flagged landmarks -- but it requires that scheme and errors on anything
with fewer than 21 landmarks; use this function instead for any other
landmark configuration, or for a lighter-weight look at FISHMORPH data
without that added detail.
}
\examples{
fish <- load_t26_saudrune_landmarks()
plot_landmarks(fish, specimen = 1)

\dontrun{
# Overlay the original photograph (requires the "jpeg" package and a
# photograph in the same pixel coordinate system as `fish`'s landmarks):
plot_landmarks(fish, specimen = 1, background_image = "specimen1.jpg")
}

}
\seealso{
\code{\link[=gpa_fish]{gpa_fish()}}, \code{\link[=morpho_space]{morpho_space()}}, \code{\link[=plot_fishmorph_points]{plot_fishmorph_points()}} (richer
viewer for FISHMORPH-scheme data specifically)
}
