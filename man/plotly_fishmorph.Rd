% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotly_fishmorph.R
\name{plotly_fishmorph}
\alias{plotly_fishmorph}
\title{Interactive FISHMORPH projection plot (plotly)}
\usage{
plotly_fishmorph(
  x,
  style = c("hull", "spider", "density", "points"),
  axes = NULL,
  reference_density = TRUE,
  reference_points = FALSE,
  itv_reference = FALSE,
  arrows = FALSE,
  arrow_scale = 0.8,
  arrow_col = "grey20",
  background = TRUE,
  background_col = "grey75",
  density_probs = c(0.25, 0.5, 0.99),
  density_palette = NULL,
  select_species = NULL,
  select_specimens = NULL,
  ellipse_level = 0.95,
  density_level = 0.95,
  hover_traits = FALSE,
  hover_font_size = 10,
  point_size = 7,
  fill_alpha = 0.2,
  equal_aspect = TRUE,
  legend = TRUE,
  legend_title = "Species",
  legend_italic = TRUE,
  abbreviate_species = TRUE,
  webgl = TRUE,
  title = NULL,
  width = NULL,
  height = NULL
)
}
\arguments{
\item{x}{An object of class \code{"intrait_fishmorph_projection"}, from
\code{\link[=project_fishmorph]{project_fishmorph()}}.}

\item{style}{Character, the per-species (foreground) geometry: \code{"hull"}
(default; convex hull = the ITV footprint), \code{"spider"} (dispersion
ellipse plus spokes to the centroid), \code{"density"} (kernel-density
contour of the projected specimens), or \code{"points"} (no per-group
geometry). As in the static method, this is independent of
\code{reference_density}, which controls the \emph{background} layer.}

\item{axes}{Integer vector of length 2, which principal components to
display. \code{NULL} (default) uses the two axes the projection was built to
plot (\code{x$axes}); any other pair is taken from the all-component scores
(\code{x$scores_all}, \code{x$global_scores_all}) with no refitting, so exploring
further components costs nothing and cannot move the space.}

\item{reference_density}{Logical, draw the reference database as a filled
kernel-density heatmap (the same white-to-dark-red ramp as the static
plot) with a few nested highest-density-region contour lines. Defaults
to \code{TRUE}.}

\item{reference_points}{Logical, draw the reference species as a light
background point cloud. Can be combined with \code{reference_density}.
Defaults to \code{FALSE}.}

\item{itv_reference}{Logical, mark the focal species' own entries in the
reference database, coloured to match their species, so the single
database morphotype can be compared with the spread of the projected
individuals. Matching is case-insensitive and treats spaces and
underscores as equivalent; species with no reference row are skipped
with a warning. Defaults to \code{FALSE}.}

\item{arrows}{Logical, overlay the trait loadings (\code{x$loadings}) as biplot
arrows from the origin, with the trait name at each tip, turning the
score plot into a PCA biplot. Drawn as plotly annotations, so they are
part of the figure but carry no hover information. Defaults to \code{FALSE}.}

\item{arrow_scale}{Numeric in \verb{(0, 1]}, how far the longest loading arrow
reaches, as a fraction of the distance from the origin to the nearest
edge of the displayed region; all arrows share that one factor, so their
relative lengths and directions are preserved. Loadings are unit-scaled
vectors with no natural size in score units, so this is a display choice
only. Defaults to \code{0.8}.}

\item{arrow_col}{Colour of the loading arrows and their labels. Defaults
to \code{"grey20"}.}

\item{background}{Logical master switch: when \code{FALSE}, no reference layer
of any kind is drawn (overrides \code{reference_density},
\code{reference_points}, \code{itv_reference}). Defaults to \code{TRUE}.}

\item{background_col}{Colour of the reference point cloud. Defaults to
\code{"grey75"}.}

\item{density_probs}{Numeric vector of coverage probabilities for the
heatmap's highest-density-region contour lines. Defaults to
\code{c(0.25, 0.5, 0.99)}.}

\item{density_palette}{Optional character vector of colours for the
heatmap gradient (low to high density). \code{NULL} (default) uses the
package's white-to-dark-red ramp.}

\item{select_species, select_specimens}{Optional character vectors
restricting \emph{this figure} to a subset of the already-projected species
or specimen identifiers, without recomputing the space (the reference
background is unaffected). Combined by intersection. \code{NULL} (default)
shows everything projected.}

\item{ellipse_level, density_level}{Coverage probabilities for the
\code{"spider"} ellipse and the \code{"density"} contour respectively. Default
\code{0.95}.}

\item{hover_traits}{Logical, add each specimen's trait values (on the
analysis scale) to its tooltip. Defaults to \code{FALSE}: nine ratios on one
line produce a tooltip wider than the plotting region, which hides the
very morphospace the point is being located in. When \code{TRUE} the values
are wrapped three per line to keep the box narrow.}

\item{hover_font_size}{Numeric, font size (in pixels) of the tooltip text.
Defaults to \code{10}, smaller than plotly's own default, since a tooltip here
carries an identifier and a species name rather than a single number.}

\item{point_size}{Numeric, marker size (in pixels) of the projected
specimens. Defaults to \code{7}.}

\item{fill_alpha}{Numeric in \verb{[0, 1]}, opacity of the per-species hull /
ellipse / contour fill. Defaults to \code{0.2}, low enough that overlapping
ITV footprints stay readable. Set to \code{0} for outlines only.}

\item{equal_aspect}{Logical, force one score unit on the horizontal axis to
occupy the same number of pixels as one score unit on the vertical axis
(plotly's \code{scaleanchor}). The two displayed components are in the same
units, so an unequal aspect ratio silently distorts every distance, hull
shape and loading direction read off the figure, and rescales them again
at every zoom; the cost is that a low-variance PC2 then occupies only a
thin horizontal band, as it should. Set to \code{FALSE} to reproduce the
base-graphics method's behaviour, where each axis is stretched
independently to fill the panel. Defaults to \code{TRUE}.}

\item{legend}{Logical, show the (clickable) species legend. Defaults to
\code{TRUE}.}

\item{legend_title}{Title above the legend. Defaults to \code{"Species"}.}

\item{legend_italic}{Logical, italicise the species names in the legend
and tooltips (as binomials), using the HTML markup plotly renders.
Defaults to \code{TRUE}.}

\item{abbreviate_species}{Logical, abbreviate the genus in the legend
(e.g. \code{"Squalius cephalus"} becomes \code{"S. cephalus"}). Tooltips always
carry the full name. Defaults to \code{TRUE}.}

\item{webgl}{Logical, render the reference point cloud with WebGL
(\code{"scattergl"}) rather than SVG. A ~9,000-point SVG trace makes the
widget sluggish in a browser and slow to save; WebGL draws it on the GPU
in one pass. Only the reference cloud is affected -- the specimens stay
SVG so their markers and hover boxes render identically to a small
figure. Defaults to \code{TRUE}.}

\item{title}{Optional plot title. \code{NULL} (default) builds one naming the
space and the style, as the static method does.}

\item{width, height}{Optional widget dimensions in pixels, passed to
\code{\link[plotly:plot_ly]{plotly::plot_ly()}}. \code{NULL} (default) lets the container decide.}
}
\value{
A \code{plotly} htmlwidget (class \code{c("plotly", "htmlwidget")}). Printed
at the console it opens in the RStudio Viewer or a browser; it can be
embedded in an R Markdown / Quarto document, or written to a
self-contained HTML file with \code{htmlwidgets::saveWidget()}. The widget's
mode-bar camera button is configured to export SVG rather than PNG, so
the figure taken out of an exploratory session is a vector graphic.
}
\description{
The interactive counterpart of \code{\link[=plot.intrait_fishmorph_projection]{plot.intrait_fishmorph_projection()}}:
the same figure -- the reference database as a kernel-density heatmap
and/or point cloud, with the projected specimens on top of it, grouped by
species -- rendered as a \strong{plotly} htmlwidget instead of a base-graphics
device. What the static plot cannot do, and what this is for, is
\emph{identifying} points: a ~9,000-species morphospace with a few hundred
projected individuals in it has no room for labels, so hovering is the
only practical way to ask which specimen (and which trait values) sits at
a given position. The legend is clickable, so single species can be
isolated from an overplotted cloud without recomputing anything, and the
plot can be zoomed into a dense region.
}
\section{What is drawn, and in which order}{

Traces are added back to front: the reference density heatmap and its
highest-density-region contour lines, the reference point cloud, the
per-species geometry (\code{style}), the focal species' own reference-database
points (\code{itv_reference}), then the projected specimens. Every trace
belonging to one species shares a plotly \code{legendgroup}, so a click on that
species in the legend hides or shows its points \emph{and} its hull/ellipse
together (double-click isolates it). The reference layers are outside any
legend group and stay visible throughout, since they are the frame of
reference the selection is read against.
}

\section{Hover information}{

Each projected point reports its specimen identifier (the row name of the
projection's \code{scores}), its species and its coordinates on the two
displayed axes. Adding \code{hover_traits = TRUE} appends its nine FISHMORPH
ratios \emph{on the analysis scale} (i.e. \code{log10(x + 1)} when
\code{\link[=project_fishmorph]{project_fishmorph()}} was called with \code{log_transform = TRUE}), taken from
\code{x$specimen_traits}, so the numbers in the tooltip are directly comparable
with the reference values that define the space rather than with the raw
ratios; they are off by default, and wrapped three per line when on,
because plotly does not wrap hover text and a nine-ratio line covers the
morphospace the point is being located in. A hull or
ellipse reports its species, its number of specimens and, when the
projection carries an \code{\link[=itv_proportion]{itv_proportion()}} result, that species' share of
the global functional volume. Reference points report the reference
species' name when the projection carries labels (\code{x$global_species}).
}

\section{Requirements}{

Needs the \strong{plotly} package (in \code{Suggests}): install it with
\code{install.packages("plotly")}. Only this function requires it; the rest of
intraitR is unaffected.
}

\examples{
\donttest{
# Needs the FISHMORPH database CSV (not shipped) and the plotly package.
ref_path <- "FishMORPH/fishmorph_data.csv"
if (file.exists(ref_path) && requireNamespace("plotly", quietly = TRUE)) {
  fish   <- load_t26_saudrune_landmarks()
  ratios <- fishmorph_ratios(fishmorph_segments(fish), na_action = "omit")
  proj   <- project_fishmorph(ratios, reference = ref_path)

  # ITV footprints over the reference heatmap, hover a point for its traits
  plotly_fishmorph(proj)

  # biplot arrows + each species' own database point, on axes 1 and 3
  plotly_fishmorph(proj, axes = c(1, 3), arrows = TRUE, itv_reference = TRUE)

  # raw reference cloud instead of the heatmap, dispersion ellipses
  plotly_fishmorph(proj, style = "spider", reference_density = FALSE,
                   reference_points = TRUE)

  # save as a standalone HTML file
  # htmlwidgets::saveWidget(plotly_fishmorph(proj), "fishmorph.html")
}
}

}
\references{
Brosse, S., Charpin, N., Su, G., Toussaint, A., Herrera-R, G. A.,
Tedesco, P. A., & Villeger, S. (2021). FISHMORPH: A global database on
morphological traits of freshwater fishes. Global Ecology and
Biogeography, 30(11), 2330-2336.

Sievert, C. (2020). Interactive Web-Based Data Visualization with R,
plotly, and shiny. Chapman and Hall/CRC.
}
\seealso{
\code{\link[=project_fishmorph]{project_fishmorph()}},
\code{\link[=plot.intrait_fishmorph_projection]{plot.intrait_fishmorph_projection()}} (the static figure),
\code{\link[=plot_fishmorph_density]{plot_fishmorph_density()}} (the one-dimensional marginals),
\code{\link[=itv_proportion]{itv_proportion()}}
}
