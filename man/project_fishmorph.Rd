% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project_fishmorph.R
\name{project_fishmorph}
\alias{project_fishmorph}
\alias{print.intrait_fishmorph_projection}
\alias{plot.intrait_fishmorph_projection}
\title{Project specimens into the global FISHMORPH functional space}
\usage{
project_fishmorph(
  specimens,
  reference,
  traits = c("REs", "VEp", "RMl", "OGp", "BEl", "BLs", "PFv", "PFs", "CPt"),
  groups = NULL,
  select_species = NULL,
  select_specimens = NULL,
  reference_prelogged = TRUE,
  specimens_prelogged = FALSE,
  log_transform = TRUE,
  scale = TRUE,
  axes = c(1, 2),
  volume_dims = 2L,
  na_action = c("omit", "fail")
)

\method{print}{intrait_fishmorph_projection}(x, ...)

\method{plot}{intrait_fishmorph_projection}(
  x,
  style = c("hull", "spider", "density", "points"),
  reference_density = TRUE,
  reference_points = FALSE,
  itv_reference = FALSE,
  arrows = FALSE,
  arrow_scale = 0.8,
  arrow_col = "grey20",
  background = TRUE,
  background_col = "grey75",
  density_probs = c(0.25, 0.5, 0.99),
  density_palette = NULL,
  select_species = NULL,
  select_specimens = NULL,
  ellipse_level = 0.95,
  density_level = 0.95,
  legend = TRUE,
  legend_position = "outside",
  legend_title = "Species",
  legend_italic = TRUE,
  abbreviate_species = TRUE,
  ...
)
}
\arguments{
\item{specimens}{A \code{data.frame} (e.g. from \code{\link[=fishmorph_ratios]{fishmorph_ratios()}}, class
\code{"intrait_fishmorph"}) with one row per specimen, containing at least
the columns named in \code{traits}, and -- for grouping/colouring -- either
a \code{species} column or a \code{groups} argument. Row names are used as
specimen identifiers (for the \code{select_specimens} filter and for the
returned scores).}

\item{reference}{The reference trait table defining the global space:
either a \code{data.frame}/\code{matrix} with (at least) the \code{traits} columns,
or a single string giving the path to a delimited file to read (a
\verb{;}-separated, \code{.}-decimal file such as the shipped \code{fishmorph_data.csv}
is read with \code{\link[utils:read.csv2]{utils::read.csv2()}}; a \verb{,}-separated file with
\code{\link[utils:read.csv]{utils::read.csv()}}). One row per reference species.}

\item{traits}{Character vector of the trait columns to use, present in
both \code{specimens} and \code{reference}. Defaults to the nine dimensionless
FISHMORPH ratios common to \code{\link[=fishmorph_ratios]{fishmorph_ratios()}} output and the
FISHMORPH database (\code{"REs"}, \code{"VEp"}, \code{"RMl"}, \code{"OGp"}, \code{"BEl"},
\code{"BLs"}, \code{"PFv"}, \code{"PFs"}, \code{"CPt"}); the two mouth ratios (\code{MBl},
\code{MBw}) are excluded by default because \code{\link[=fishmorph_ratios]{fishmorph_ratios()}} does not
produce them.}

\item{groups}{Optional factor (or character vector), one value per row of
\code{specimens}, used to colour/group the projected points. If \code{NULL} and
\code{specimens} has a \code{species} column, that column is used.}

\item{select_species}{Optional character vector: keep only these
species/groups among the projected specimens (matched against
\code{groups}). \code{NULL} (default) keeps them all. Unmatched names are
reported with a warning.}

\item{select_specimens}{Optional character vector: keep only these
specimen identifiers (matched against \code{rownames(specimens)}). \code{NULL}
(default) keeps them all. Combined with \code{select_species} by
intersection (a specimen must satisfy both filters to be kept).}

\item{reference_prelogged}{Logical; is \code{reference} already
\code{log10(x + 1)}-transformed? Defaults to \code{TRUE} (the state in which the
FISHMORPH database is distributed). When \code{TRUE}, no log transform is
applied to the reference even if \code{log_transform = TRUE}.}

\item{specimens_prelogged}{Logical; are the \code{specimens}' trait values
already \code{log10(x + 1)}-transformed? Defaults to \code{FALSE}
(\code{\link[=fishmorph_ratios]{fishmorph_ratios()}} returns raw ratios).}

\item{log_transform}{Logical, apply a \code{log10(x + 1)} transform to bring
raw inputs onto the (log) reference scale, following
\code{\link[=trait_space]{trait_space()}}'s default preprocessing. Defaults to \code{TRUE}. Applied to
the reference only if not \code{reference_prelogged}, and to the specimens
only if not \code{specimens_prelogged}, so that both end up on the same
scale exactly once.}

\item{scale}{Logical, standardise traits to unit variance inside the PCA
(centring is always applied). Defaults to \code{TRUE}, matching
\code{\link[=trait_space]{trait_space()}}.}

\item{axes}{Integer vector of length 2, the ordination axes retained for
the returned scores and plotting. Defaults to \code{c(1, 2)}.}

\item{volume_dims}{Integer, the number of leading principal components on
which the \emph{functional-volume} proportion (convex-hull ratio) is computed
by the bundled \code{\link[=itv_proportion]{itv_proportion()}} call. Independent of \code{axes} (which only
controls the two plotted axes). Defaults to \code{2L}; a convex hull in
\code{volume_dims} dimensions needs at least \code{volume_dims + 1} non-degenerate
points, so per-species volumes are \code{NA} for species with too few
specimens.}

\item{na_action}{Character, how to handle specimens with missing values
in the \code{traits} columns (a projection cannot place a row with a missing
coordinate): \code{"omit"} (default) drops them, reporting how many;
\code{"fail"} stops with an error. Reference rows with missing trait values
are always dropped (with a message) before the space is built.}

\item{x}{An object of class \code{"intrait_fishmorph_projection"}.}

\item{...}{Further arguments passed to \code{\link[graphics:plot]{graphics::plot()}} (for the
\code{plot()} method) or currently unused (for \code{print()}).}

\item{style}{Character, one of \code{"hull"} (default; per-species convex hull
= the ITV footprint), \code{"spider"} (dispersion ellipse + spokes to the
centroid), \code{"density"} (per-species kernel-density contour of the
projected specimens), or \code{"points"} (the projected points only, no
per-group geometry). Note this is the \emph{foreground} per-species geometry
and is independent of \code{reference_density}, which controls the
\emph{background} reference heatmap.}

\item{reference_density}{Logical, draw the reference database's
distribution as a filled kernel-density heatmap (a white-to-red
gradient with a few nested highest-density-region contour lines) behind
the projected specimens -- the recommended way to show a ~9,000-species
morphospace, which is unreadable as raw points. Defaults to \code{TRUE}.}

\item{reference_points}{Logical, draw the reference database species as a
light background point cloud (the previous default display). Can be
combined with \code{reference_density}. Defaults to \code{FALSE}.}

\item{itv_reference}{Logical, mark the focal species' \emph{own} entries in
the reference database -- i.e. the FISHMORPH points of exactly the
species for which projected ITV is shown -- as filled circles coloured
to match their species, so the single database morphotype can be
compared with the spread of the projected individuals. Requires the
projection to carry reference species labels (\code{x$global_species}, added
by \code{\link[=project_fishmorph]{project_fishmorph()}}); species with no matching reference row are
skipped with a warning. Defaults to \code{FALSE}.}

\item{arrows}{Logical, overlay the trait \emph{loadings} (the PCA variable
contributions stored in \code{x$loadings}) as a biplot: one arrow per trait,
drawn from the origin in the direction of that trait's loading on the
two plotted axes, with the trait name at the arrow tip. This turns the
score plot into a standard PCA biplot, showing which morphological
ratios drive each axis and hence how a group's position in the
morphospace should be read ecomorphologically (e.g. an ITV footprint
stretched along the body-elongation arrow varies mostly in body shape).
The arrows are a purely visual overlay and do not change the ordination.
Defaults to \code{FALSE}.}

\item{arrow_scale}{Numeric in \verb{(0, 1]}, how far the longest loading arrow
reaches, as a fraction of the distance from the origin to the nearest
plot edge; all arrows are scaled by this same factor so their relative
lengths and directions are preserved. Loadings are unit-scaled vectors
with no natural size in score units, so this is a display choice only.
Smaller values keep the arrows clear of the outer points; larger values
make short loadings more legible. Only used when \code{arrows = TRUE}.
Defaults to \code{0.8}.}

\item{arrow_col}{Colour of the loading arrows and their trait labels (when
\code{arrows = TRUE}). Defaults to \code{"grey20"}.}

\item{background}{Logical master switch: when \code{FALSE}, no reference
background layer of any kind is drawn (overrides \code{reference_density},
\code{reference_points} and \code{itv_reference}). Defaults to \code{TRUE}.}

\item{background_col}{Colour of the reference point cloud (when
\code{reference_points = TRUE}). Defaults to \code{"grey75"}.}

\item{density_probs}{Numeric vector of coverage probabilities for the
reference heatmap's highest-density-region contour lines. Defaults to
\code{c(0.25, 0.5, 0.99)}.}

\item{density_palette}{Optional character vector of colours for the
reference heatmap gradient (low to high density). \code{NULL} (default) uses
a white-to-dark-red ColorBrewer YlOrRd ramp.}

\item{ellipse_level, density_level}{Coverage probabilities for the
\code{"spider"} ellipse and \code{"density"} contour respectively. Default
\code{0.95}.}

\item{legend}{Logical, draw a species legend. Defaults to \code{TRUE}.}

\item{legend_position, legend_title, legend_italic, abbreviate_species}{Legend controls, as in \code{\link[=plot.intrait_traitspace]{plot.intrait_traitspace()}}; defaults here are
tuned for species names (\code{legend_title = "Species"}, italic,
abbreviated binomials).}
}
\value{
An object of class \code{"intrait_fishmorph_projection"}, a list with
elements \code{scores} (data.frame of projected specimen scores on the two
axes), \code{global_scores} (data.frame of the reference species' own scores
on the same axes, for the background cloud), \code{global_species} (character
vector of the reference species labels, aligned row-for-row to
\code{global_scores}; from a \code{"Species"} column of \code{reference} when present,
else its row names -- used by the plot method to locate the focal
species' own reference points), \code{groups} (factor aligned
to \code{scores}), \code{var_explained} (percent variance of the two axes),
\code{loadings} (PCA variable loadings), \code{axes}, \code{traits}, \code{n_reference}
(number of reference species used), and \code{pca} (the fitted
\code{\link[stats:prcomp]{stats::prcomp()}} object, so further specimens can be projected with
\code{predict(x$pca, ...)}). It also carries the material used to quantify how
much of the global functional diversity the projected ITV occupies:
\code{scores_all}/\code{global_scores_all} (specimen and reference scores on \emph{all}
components), \code{specimen_traits} (the transformed specimen traits, analysis
scale), \code{trait_ranges_reference} (per-trait reference envelope),
\code{reference_traits} (the full reference trait matrix on the analysis scale,
one row per reference species -- used e.g. by
\code{\link[=plot_fishmorph_density]{plot_fishmorph_density()}} to draw the reference trait distributions),
\code{volume_dims}, and \code{itv_proportion} -- an \code{"intrait_itv_proportion"}
object (see \code{\link[=itv_proportion]{itv_proportion()}}) giving the ITV-to-global proportion per
trait and per functional volume, pooled and per species. Has dedicated
\code{\link[=print]{print()}} and \code{\link[=plot]{plot()}} methods.

\code{print()} invisibly returns \code{x}.

\code{plot()} invisibly returns \code{x}.
}
\description{
Builds a fixed functional trait space by Principal Component Analysis of
a reference database of FISHMORPH ecomorphological ratios (typically the
full FISHMORPH database of Brosse et al. 2021, ~9,000 species), then
places new specimens -- e.g. the individuals of a few focal species,
from \code{\link[=fishmorph_ratios]{fishmorph_ratios()}} -- into that \emph{same, frozen} space using
\code{\link[stats:predict]{stats::predict()}}, without re-estimating the ordination. This is the
standard way to show how much of the global morphospace a group's
intraspecific trait variation (ITV) occupies relative to the entire
diversity of fishes, on axes defined once by the reference alone (so the
space does not move when specimens are added or removed, and separate
studies remain comparable).

Draws the reference database as a background layer -- by default a
kernel-density heatmap of the whole FISHMORPH morphospace -- with the
projected specimens on top of it, grouped/coloured by species, in any of
the display styles shared with \code{\link[=plot.intrait_traitspace]{plot.intrait_traitspace()}}. The
background can equivalently (or additionally) be shown as the raw
reference point cloud, and the focal species' own positions \emph{in the
reference database} can be marked, to see how a group's projected
intraspecific trait variation (ITV) sits relative to its single
database point.
}
\details{
In the \code{plot()} method, \code{select_species}/\code{select_specimens} restrict
\emph{this plot} to a subset of the already-projected species or specimen
identifiers, without recomputing the space (the reference background is
unchanged); \code{NULL} (default) plots everything projected. When
\code{itv_reference = TRUE}, the reference points marked follow the same
selection: only the focal species still shown after
\code{select_species}/\code{select_specimens} are matched against the reference
database (matching is case-insensitive and treats spaces and underscores
as equivalent, so \code{"Squalius cephalus"} and \code{"Squalius_cephalus"} match).
}
\section{Scale of the reference vs the specimens}{

The published FISHMORPH database distributes its trait columns already
\code{log10(x + 1)}-transformed (verifiable against the raw measurements: a
stored body elongation of \code{0.888} is \code{log10(6.72 + 1)}, not the raw
\code{Bl / Bd = 6.72}), whereas \code{\link[=fishmorph_ratios]{fishmorph_ratios()}} returns \emph{raw} ratios. If
the reference were log-transformed a second time while the specimens
were logged only once (or vice versa), the two sets of scores would land
on incompatible scales and the projected specimens would be displaced by
a large, spurious offset. The defaults here encode the correct
combination for that common case (\code{reference_prelogged = TRUE},
\code{specimens_prelogged = FALSE}, \code{log_transform = TRUE}): the reference is
taken as-is and the specimens are \code{log10(x + 1)}-transformed onto its
scale before projection. Set these flags explicitly if your inputs are
on different scales (e.g. a raw reference: \code{reference_prelogged = FALSE}).
}

\examples{
\donttest{
# Reference: the FISHMORPH database (a ~9,000-species CSV, not shipped with
# the package). The example runs only where that file is available locally.
ref_path <- "FishMORPH/fishmorph_data.csv"
if (file.exists(ref_path)) {
  # Focal specimens: the T-26 Saudrune individuals
  fish   <- load_t26_saudrune_landmarks()
  ratios <- fishmorph_ratios(fishmorph_segments(fish), na_action = "omit")

  proj <- project_fishmorph(ratios, reference = ref_path)
  proj

  plot(proj, style = "hull")       # ITV footprints over the density heatmap
  plot(proj, style = "spider")     # dispersion around centroids
  plot(proj, style = "points", select_species = "Squalius cephalus")

  # raw reference cloud instead of (or as well as) the heatmap:
  plot(proj, style = "hull", reference_density = FALSE, reference_points = TRUE)

  # mark each focal species' own FISHMORPH-database point:
  plot(proj, style = "hull", itv_reference = TRUE)

  # overlay the trait loadings as biplot arrows:
  plot(proj, style = "hull", arrows = TRUE)
}
}

}
\references{
Brosse, S., Charpin, N., Su, G., Toussaint, A., Herrera-R, G. A.,
Tedesco, P. A., & Villeger, S. (2021). FISHMORPH: A global database on
morphological traits of freshwater fishes. Global Ecology and
Biogeography, 30(11), 2330-2336.
}
\seealso{
\code{\link[=fishmorph_ratios]{fishmorph_ratios()}}, \code{\link[=trait_space]{trait_space()}},
\code{\link[=plot.intrait_fishmorph_projection]{plot.intrait_fishmorph_projection()}}
}
