% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_landmarks_csv.R
\name{read_landmarks_csv}
\alias{read_landmarks_csv}
\title{Import landmark coordinates from a generic long-format CSV file}
\usage{
read_landmarks_csv(
  file,
  specimen = "specimen",
  landmark = "landmark",
  coords = c("X", "Y"),
  metadata = NULL,
  ...
)
}
\arguments{
\item{file}{Character. Path to a CSV file, or a \code{data.frame} already
loaded in R.}

\item{specimen}{Character. Name of the column identifying specimens.
Defaults to \code{"specimen"}.}

\item{landmark}{Character. Name of the column identifying landmarks
(used only to order coordinates consistently within a specimen).
Defaults to \code{"landmark"}.}

\item{coords}{Character vector of column names holding the coordinate
values, in order (e.g. \code{c("X", "Y")} for 2D or \code{c("X", "Y", "Z")} for
3D). Defaults to \code{c("X", "Y")}.}

\item{metadata}{Optional \code{data.frame} of specimen-level metadata, as in
\code{\link[=read_tps]{read_tps()}}.}

\item{...}{Additional arguments passed to \code{\link[utils:read.csv]{utils::read.csv()}} when
\code{file} is a path (ignored when \code{file} is already a \code{data.frame}).}
}
\value{
An object of class \code{"intrait_landmarks"} (see \code{\link[=read_tps]{read_tps()}} for
details); \code{scale} is set to \code{NULL} since long-format CSV files do not
carry a digitization scale.
}
\description{
Reads landmark coordinates stored in a "long" (tidy) CSV file, with one
row per specimen/landmark combination, and reshapes them into a
geomorph-style \verb{p x k x n} array. Useful when landmarks were digitized
outside \code{tpsDig} (e.g. in ImageJ/Fiji, or exported from a database), or
for three-dimensional landmark configurations.
}
\examples{
df <- data.frame(
  specimen = rep(c("fish_01", "fish_02"), each = 3),
  landmark = rep(1:3, times = 2),
  X = c(10, 15, 20, 11, 16, 21),
  Y = c(20, 25, 20, 21, 26, 21)
)
lm <- read_landmarks_csv(df)
dim(lm$coords)

}
\seealso{
\code{\link[=read_tps]{read_tps()}}, \code{\link[=gpa_fish]{gpa_fish()}}
}
