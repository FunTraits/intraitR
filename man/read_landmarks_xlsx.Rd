% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_landmarks_xlsx.R
\name{read_landmarks_xlsx}
\alias{read_landmarks_xlsx}
\title{Import landmark coordinates from a generic "wide"-format Excel sheet}
\usage{
read_landmarks_xlsx(
  file,
  sheet = 1,
  n_landmarks,
  x_pattern = "X_{i}",
  y_pattern = "Y_{i}",
  id_cols = "Code",
  specimen = NULL,
  species_file = NULL,
  species_sheet = 1,
  species_by = NULL,
  metadata = NULL,
  ...
)
}
\arguments{
\item{file}{Character. Path to an \code{.xlsx}/\code{.xls} file.}

\item{sheet}{Sheet to read: a name or 1-based index, passed to
\code{\link[readxl:read_excel]{readxl::read_excel()}}. Defaults to the first sheet.}

\item{n_landmarks}{Integer, the number of landmarks digitized per
specimen (i.e. the number of X/Y column pairs to look for).}

\item{x_pattern, y_pattern}{Character, a template for the X/Y column names
of landmark \code{i}, with \code{"{i}"} as a placeholder for the landmark number.
Defaults to \code{"X_{i}"}/\code{"Y_{i}"} (e.g. \verb{X_1, X_2, ..., Y_1, Y_2, ...},
the layout of a single-digitization sheet); use \code{"{i}_X"}/\code{"{i}_Y"} for
a sheet organised the other way round (e.g. \verb{1_X, 1_Y, 2_X, 2_Y, ...},
as is common in replicate-digitization/repeatability sheets).}

\item{id_cols}{Character vector of one or more column names that
together identify each row (e.g. a specimen code, and, if the sheet
records more than one digitization per specimen, an operator or
replicate/measurement column). Kept as-is in \code{metadata}. Rows with a
missing value in \code{id_cols[1]} (typically a blank spreadsheet row) are
dropped with a message.}

\item{specimen}{\code{NULL} (default), or a single existing column name to use
directly as the specimen identifier. When \code{NULL}, specimen identifiers
are built by pasting together every column in \code{id_cols} with \code{"_"}
(e.g. \code{code_operator}, or \code{code_replicate}), which is almost always
the desired behaviour when a sheet records more than one digitization
per specimen (each row must resolve to a unique identifier). Duplicated
identifiers are made unique (with a warning) via \code{\link[=make.unique]{make.unique()}}.}

\item{species_file, species_sheet}{Optional path (and sheet, as \code{sheet}
above) to a second spreadsheet of one-row-per-specimen metadata (e.g.
species identification) to left-join onto the landmark data.}

\item{species_by}{Character, the column name shared by \code{file} and
\code{species_file} to join on. Defaults to \code{id_cols[1]} (typically the
specimen code column). Every column of \code{species_file} other than
\code{species_by} is added to \code{metadata} as-is (no renaming, no attempt to
resolve conflicting/uncertain identifications -- see Details).}

\item{metadata}{Optional further \code{data.frame} of specimen-level
metadata, merged in on top of \code{id_cols}/\code{species_file}, as in
\code{\link[=read_tps]{read_tps()}}.}

\item{...}{Additional arguments passed to \code{\link[readxl:read_excel]{readxl::read_excel()}} (e.g.
\code{skip}, \code{na}, \code{col_types}).}
}
\value{
An object of class \code{"intrait_landmarks"} (see \code{\link[=read_tps]{read_tps()}} for
details); \code{scale} is set to \code{NULL} since a wide landmark sheet does not,
by itself, carry a digitization scale (see \code{\link[=fishmorph_segments]{fishmorph_segments()}}'s
\code{scale_cm} argument if landmarks 20-21 encode a calibration segment, as
in the FISHMORPH scheme).
}
\description{
Reads landmark coordinates stored in a "wide" spreadsheet layout, one row
per specimen (or per replicate digitization) with one pair of X/Y columns
per landmark (e.g. \verb{X_1, Y_1, X_2, Y_2, ...}, or \verb{1_X, 1_Y, 2_X, 2_Y, ...}),
and reshapes them into a geomorph-style \verb{p x k x n} array. This is the
layout produced directly by most manual digitization spreadsheets (one
column per landmark coordinate, filled in by hand or copy-pasted from
image analysis software), as opposed to the "long" (tidy) layout expected
by \code{\link[=read_landmarks_csv]{read_landmarks_csv()}}. An optional second spreadsheet of specimen-level
identifications/metadata (e.g. taxonomic identification, capture date) can
be joined in directly via \code{species_file}.
}
\details{
This function generalises the private, one-off cleaning script originally
written to import the real T-26 Saudrune field data (\code{Code}/\code{Utilisateur}
columns, \code{X_1..X_21}/\code{Y_1..Y_21}; see \code{data-raw/t26_saudrune_prepare.R}
and \code{\link[=load_t26_saudrune_landmarks]{load_t26_saudrune_landmarks()}}) so that the same "wide spreadsheet"
import can be reused directly on new field seasons or other surveys,
without hand-written reshaping code, whatever the specific column-naming
convention (\code{x_pattern}/\code{y_pattern}) or number of landmarks
(\code{n_landmarks}) involved.

Coordinate cells are coerced with \code{\link[=as.numeric]{as.numeric()}}; spreadsheet cells that
are blank or contain the literal text \code{"NA"} become \code{NA} silently (the
normal, expected case for genuinely missing digitizations), while any
other non-numeric cell content triggers a warning naming the offending
column(s), since that usually indicates a data-entry problem (a stray
comment, a mistyped value) rather than an intentionally missing landmark.

\code{species_file} performs a plain left-join (via \code{\link[=match]{match()}}, so \code{file}'s row
order and length are always preserved even if \code{species_file} has
duplicated or missing keys) and does \strong{not} attempt to reproduce any
project-specific identification-resolution logic (e.g. falling back from
a preliminary/AI-assisted call to a curated one, or flagging uncertain
identifications) -- if your identification sheet needs that kind of
resolution before use, do it in R first (e.g. with \code{\link[dplyr:coalesce]{dplyr::coalesce()}}
or a custom \code{data.frame} you then pass to \code{species_file}/\code{metadata}), or
inspect the FISHMORPH T-26 case study in \code{data-raw/t26_saudrune_prepare.R}
for a worked, non-generic example of exactly that kind of resolution.
Likewise, \code{species_by} values are matched exactly (after \code{\link[=trimws]{trimws()}}):
inconsistent codes across the two files (extra whitespace, a stray
\code{"_"} vs \code{"-"}, a parenthetical annotation) will not be matched, and
should be normalised in \code{file}/\code{species_file} beforehand if needed.
}
\examples{
if (requireNamespace("readxl", quietly = TRUE) &&
    requireNamespace("writexl", quietly = TRUE)) {
  wide <- data.frame(
    Code = c("fish_01", "fish_02", "fish_03"),
    Utilisateur = c("Op1", "Op1", "Op2"),
    X_1 = c(10, 11, 9), Y_1 = c(20, 21, 19),
    X_2 = c(15, 16, 14), Y_2 = c(25, 26, 24),
    X_3 = c(20, 21, 19), Y_3 = c(20, 21, 19)
  )
  ident <- data.frame(
    Code = c("fish_01", "fish_02", "fish_03"),
    species = c("Gobio occitaniae", "Gobio occitaniae", "Squalius cephalus")
  )
  xlsx_path <- tempfile(fileext = ".xlsx")
  ident_path <- tempfile(fileext = ".xlsx")
  writexl::write_xlsx(wide, xlsx_path)
  writexl::write_xlsx(ident, ident_path)

  lm <- read_landmarks_xlsx(
    xlsx_path, n_landmarks = 3, id_cols = c("Code", "Utilisateur"),
    species_file = ident_path, species_by = "Code"
  )
  dim(lm$coords)
  lm$metadata
}

}
\seealso{
\code{\link[=read_landmarks_csv]{read_landmarks_csv()}}, \code{\link[=read_tps]{read_tps()}}, \code{\link[=gpa_fish]{gpa_fish()}},
\code{\link[=load_t26_saudrune_landmarks]{load_t26_saudrune_landmarks()}}
}
