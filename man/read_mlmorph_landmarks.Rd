% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_mlmorph_landmarks.R
\name{read_mlmorph_landmarks}
\alias{read_mlmorph_landmarks}
\title{Import predicted landmarks from an ml-morph / landmarking-app measure table}
\usage{
read_mlmorph_landmarks(
  file,
  specimen = "specimen",
  landmark = "landmark",
  coords = c("X", "Y"),
  scale_col = "mm_per_px",
  metadata = NULL,
  by = NULL,
  operator = "ml_morph",
  replicate = 1L,
  save_to = NULL,
  ...
)
}
\arguments{
\item{file}{Character path to the measure table (a CSV as written by the
predictor or the landmarking app), or a \code{data.frame} already loaded in
R.}

\item{specimen, landmark}{Character, the columns identifying specimens and
landmarks. Default \code{"specimen"} and \code{"landmark"}.}

\item{coords}{Character vector of the coordinate columns, in order.
Defaults to \code{c("X", "Y")}.}

\item{scale_col}{Character or \code{NULL}, the column holding the per-image
calibration scale (millimetres per pixel). When present, one value per
specimen is carried into \code{metadata$mm_per_px} and a logical
\code{metadata$has_scalebar} flags specimens that had a usable scale. Set to
\code{NULL} to ignore any such column. Defaults to \code{"mm_per_px"}.}

\item{metadata}{Optional specimen-level metadata: a \code{data.frame} (or a CSV
path) with one row per individual, joined onto the imported specimens by
\code{by}. Typically an identifications table carrying \code{species} and other
descriptors.}

\item{by}{Character, the column of \code{metadata} whose values match the
\code{specimen} ids (e.g. a \code{"code"} column). \code{NULL} (default) auto-detects
the first of \code{specimen}, \code{"code"}, \code{"individual"} or \code{"id"} present in
\code{metadata}.}

\item{operator}{Character scalar recorded in \code{metadata$operator} to
identify the source of the digitization (the predictor, an app session,
an operator name). Treating a set of predicted landmarks as one
"operator" mirrors \code{\link[=load_t26_saudrune_landmarks]{load_t26_saudrune_landmarks()}} and makes it easy to
compare predicted against hand-digitized trait spaces. Defaults to
\code{"ml_morph"}.}

\item{replicate}{Integer scalar recorded in \code{metadata$replicate} (one
digitization per individual by default). Defaults to \code{1L}.}

\item{save_to}{Optional character path; when supplied, the resulting
object is also written there with \code{\link[=saveRDS]{saveRDS()}} (a convenience for the
common "predict, convert, cache" workflow). Defaults to \code{NULL} (return
only, the standard behaviour of the package's \verb{read_*()} importers).}

\item{...}{Additional arguments passed to \code{\link[utils:read.csv]{utils::read.csv()}} when \code{file}
(or \code{metadata}) is a path.}
}
\value{
An object of class \code{"intrait_landmarks"} (a \verb{p x k x n}
coordinate array plus a \code{metadata} data.frame), in the same format as
\code{\link[=simulate_fishmorph_points]{simulate_fishmorph_points()}} and \code{\link[=load_t26_saudrune_landmarks]{load_t26_saudrune_landmarks()}}. The
\code{metadata} always carries \code{specimen}, \code{individual}, \code{species},
\code{population}, \code{replicate} and \code{operator} columns (the intraitR
convention; \code{species}/\code{population} are \code{NA} when not supplied via
\code{metadata}), any extra columns joined from \code{metadata}, and -- when
\code{scale_col} is present -- \code{mm_per_px} and \code{has_scalebar}.
}
\description{
Converts a "long" (tidy) table of landmark coordinates -- as produced by
the \strong{ml-morph} shape predictor or by the interactive landmarking Shiny
app, both of which export the same schema (one row per
specimen/landmark, columns \code{specimen}, \code{landmark}, \code{X}, \code{Y} and an
optional per-image scale \code{mm_per_px}) -- straight into an
\code{"intrait_landmarks"} object, ready for \code{\link[=fishmorph_segments]{fishmorph_segments()}},
\code{\link[=fishmorph_ratios]{fishmorph_ratios()}}, \code{\link[=trait_space]{trait_space()}}, \code{\link[=itv_index]{itv_index()}},
\code{\link[=plot_fishmorph_points]{plot_fishmorph_points()}}, and the rest of the intraitR pipeline.
}
\details{
This is a thin, reusable wrapper around \code{\link[=read_landmarks_csv]{read_landmarks_csv()}} that adds
the two things those exports carry which a bare coordinate reshape does
not: the per-specimen calibration scale (\code{mm_per_px}, together with a
\code{has_scalebar} flag flagging individuals whose calibration mire -- the
FISHMORPH scale-bar landmarks 20-21 -- was absent, so that
\code{mm_per_px = NA}), and an optional join to a specimen-level metadata table
(species, stage, site, ...). Individuals without a scale keep \code{NA}
coordinates where landmarks were not placed and an \code{NA} scale; convert
pixel distances to length units downstream with the \code{scale_cm} argument of
\code{\link[=fishmorph_segments]{fishmorph_segments()}}, and impute what is missing with
\code{\link[=impute_landmarks]{impute_landmarks()}} if a complete configuration is required.
}
\examples{
# A minimal ml-morph / app-style export (long format, with a scale):
mes <- data.frame(
  specimen  = rep(c("fish_01", "fish_02"), each = 3),
  landmark  = rep(1:3, times = 2),
  X = c(10, 15, 20, 11, 16, 21),
  Y = c(20, 25, 20, 21, 26, 21),
  mm_per_px = c(0.22, 0.22, 0.22, NA, NA, NA)  # fish_02 lacks a scale bar
)
lm <- read_mlmorph_landmarks(mes)
dim(lm$coords)
lm$metadata[c("specimen", "operator", "mm_per_px", "has_scalebar")]

# Joining a species table by an identifications `code`:
ident <- data.frame(code = c("fish_01", "fish_02"),
                     species = c("Gobio occitaniae", "Squalius cephalus"))
lm2 <- read_mlmorph_landmarks(mes, metadata = ident, by = "code")
table(lm2$metadata$species, useNA = "ifany")

}
\seealso{
\code{\link[=read_landmarks_csv]{read_landmarks_csv()}}, \code{\link[=read_tps]{read_tps()}},
\code{\link[=load_t26_saudrune_landmarks]{load_t26_saudrune_landmarks()}}, \code{\link[=fishmorph_segments]{fishmorph_segments()}},
\code{\link[=impute_landmarks]{impute_landmarks()}}
}
