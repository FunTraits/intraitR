% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_tps.R
\name{read_tps}
\alias{read_tps}
\title{Import landmark coordinates from a tpsDig (\code{.tps}) file}
\usage{
read_tps(
  file,
  specID = c("imageID", "ID", "None"),
  metadata = NULL,
  negNA = FALSE
)
}
\arguments{
\item{file}{Character. Path to a \code{.tps} file.}

\item{specID}{Character, one of \code{"imageID"} (default), \code{"ID"}, or
\code{"None"}. Controls how specimen identifiers are built: from the
\verb{IMAGE=} field, from the \verb{ID=} field, or as sequential
\verb{specimen_1, specimen_2, ...} labels.}

\item{metadata}{Optional \code{data.frame} of specimen-level metadata. Row
names, or a column named \code{specimen}, must match the specimen
identifiers described by \code{specID}.}

\item{negNA}{Logical, defaults to \code{FALSE}. If \code{TRUE}, negative
coordinates (commonly used to flag missing landmarks in \code{tpsDig}) are
converted to \code{NA}.}
}
\value{
An object of class \code{"intrait_landmarks"}, a list with elements:
\describe{
\item{coords}{a \verb{p x k x n} numeric array of raw (un-aligned)
landmark coordinates, \code{p} landmarks by \code{k} (2) dimensions by \code{n}
specimens, following the \pkg{geomorph} array convention.}
\item{scale}{a named numeric vector of scale factors (real-world
units per pixel), one per specimen, taken from the \verb{SCALE=} field
of the TPS file (\code{NA} where absent).}
\item{metadata}{the merged specimen metadata \code{data.frame}, or \code{NULL}
if \code{metadata} was not supplied.}
}
}
\description{
Reads a \code{tpsDig}/\code{tpsUtil}-formatted \code{.tps} file containing two-dimensional
landmark coordinates for a set of specimens, and returns them as a
geomorph-style array, optionally merged with specimen-level metadata
(species, population, standard length, etc.) and together with the
digitization scale (units per pixel) when present in the file.
}
\details{
TPS files are the de facto exchange format for digitized landmark data in
geometric morphometrics (Rohlf, 2015). All specimens in the file must
share the same number of landmarks (\verb{LM=}); \code{read_tps()} throws an
informative error otherwise, since a common landmark configuration is
required for any downstream Procrustes analysis. Three-dimensional
(\verb{LM3=}) TPS files are not currently supported; use
\code{\link[=read_landmarks_csv]{read_landmarks_csv()}} for 3D data.
}
\examples{
tps_path <- tempfile(fileext = ".tps")
writeLines(c(
  "LM=3", "10.0 20.0", "15.0 25.0", "20.0 20.0",
  "IMAGE=fish_01.jpg", "ID=1", "SCALE=0.05",
  "LM=3", "11.0 21.0", "16.0 26.0", "21.0 21.0",
  "IMAGE=fish_02.jpg", "ID=2", "SCALE=0.05"
), tps_path)
lm <- read_tps(tps_path)
dim(lm$coords)
lm$scale

}
\references{
Rohlf FJ (2015). The tps series of software. Hystrix, 26(1), 9-12.
}
\seealso{
\code{\link[=read_landmarks_csv]{read_landmarks_csv()}}, \code{\link[=gpa_fish]{gpa_fish()}}, \code{\link[=simulate_fish_landmarks]{simulate_fish_landmarks()}}
}
