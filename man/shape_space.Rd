% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shape_space.R
\name{shape_space}
\alias{shape_space}
\alias{print.intrait_shapespace}
\title{Build a shape space from Procrustes shape coordinates}
\usage{
shape_space(gpa, groups = NULL, axes = c(1, 2))

\method{print}{intrait_shapespace}(x, ...)
}
\arguments{
\item{gpa}{An object of class \code{"intrait_gpa"}, as returned by
\code{\link[=gpa_fish]{gpa_fish()}}.}

\item{groups}{Optional factor (or character vector), one value per
specimen in the same order as \code{dimnames(gpa$coords)[[3]]}, used to
colour/group specimens when plotting. If \code{NULL} and \code{gpa$metadata}
contains a \code{species} column, it is used automatically.}

\item{axes}{Integer vector of length 2, the principal components to
retain for plotting (defaults to \code{c(1, 2)}).}

\item{x}{An object of class \code{"intrait_shapespace"}, as returned by
\code{\link[=shape_space]{shape_space()}}.}

\item{...}{Currently unused.}
}
\value{
An object of class \code{"intrait_shapespace"}, a list with
elements \code{scores} (data.frame of PC scores), \code{sdev} (standard
deviations of all PCs), \code{var_explained} (percent variance explained by
the two selected axes), \code{rotation} (PCA loadings), \code{groups}, and
\code{axes}. Has a dedicated \code{\link[=plot]{plot()}} method.

Invisibly returns \code{x}.
}
\description{
Performs a Principal Component Analysis of Procrustes-aligned shape
coordinates to construct a shape space, the
standard ordination used to visualise and compare shape variation among
specimens, populations or species in geometric morphometrics.
}
\examples{
# real T-26 Saudrune data (see ?fishmorph_shape_landmarks for why the
# scale bar and incomplete/unidentified specimens are dropped first):
fish <- load_t26_saudrune_landmarks()
fish_shape <- fishmorph_shape_landmarks(fish)
gpa <- gpa_fish(fish_shape)
ms <- shape_space(gpa, groups = fish_shape$metadata$species)
ms
\donttest{
plot(ms)
}

}
\seealso{
\code{\link[=gpa_fish]{gpa_fish()}}, \code{\link[=intraspecific_variability]{intraspecific_variability()}}
}
