% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_fish_landmarks.R
\name{simulate_fish_landmarks}
\alias{simulate_fish_landmarks}
\title{Simulate a fish landmark data set}
\usage{
simulate_fish_landmarks(
  n_per_species = 20,
  species = c("Species_A", "Species_B", "Species_C"),
  n_landmarks = 12,
  n_replicates = 3,
  seed = 123
)
}
\arguments{
\item{n_per_species}{Integer, number of individuals simulated per
species. Defaults to \code{20}.}

\item{species}{Character vector of species labels. Defaults to
\code{c("Species_A", "Species_B", "Species_C")}.}

\item{n_landmarks}{Integer, number of landmarks per configuration
(placed at evenly spaced angles around a fish-body-shaped ellipse).
Defaults to \code{12}.}

\item{n_replicates}{Integer, number of digitization replicates
simulated per individual (\code{1} for no replication). Defaults to \code{3}.}

\item{seed}{Integer or \code{NULL}. Random seed for reproducibility. Defaults
to \code{123}.}
}
\value{
An object of class \code{"intrait_landmarks"} (see \code{\link[=read_tps]{read_tps()}}),
with \code{metadata} columns \code{specimen}, \code{species}, \code{population},
\code{standard_length_mm} and \code{replicate}. \code{scale} is set to \code{1} for every
specimen (coordinates are already expressed in simulated millimetres).
}
\description{
Generates a simulated two-dimensional landmark data set representing
several fish "species" with distinct mean body shapes and sizes, several
individuals per species drawn from a population, and (optionally)
several digitization replicates per individual. This is used throughout
the package documentation and vignette to demonstrate the full
\code{intraitR} workflow without requiring real digitized data, and can be
used as a starting point (with \code{set.seed()}) for teaching or for
testing analysis code before applying it to real specimens.
}
\details{
Shape is simulated as isotropic Gaussian noise added to a per-species
mean ellipse, plus independent per-individual and per-digitization-replicate
noise components; centroid size ( proxy for standard length) is drawn
from a normal distribution. Because the per-replicate noise is
deliberately much smaller than the per-individual and per-species
variance components, the simulated data set is well suited to
illustrate \code{\link[=intraspecific_variability]{intraspecific_variability()}} (genuine among-individual and
among-species variation) and \code{\link[=measurement_error]{measurement_error()}} (small, quantifiable
digitization noise).
}
\examples{
fish <- simulate_fish_landmarks(n_per_species = 5, n_replicates = 2)
fish
head(fish$metadata)

}
\seealso{
\code{\link[=read_tps]{read_tps()}}, \code{\link[=gpa_fish]{gpa_fish()}}
}
