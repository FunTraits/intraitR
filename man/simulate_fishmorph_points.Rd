% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_fishmorph_points.R
\name{simulate_fishmorph_points}
\alias{simulate_fishmorph_points}
\title{Simulate landmark data following the FISHMORPH point digitization scheme}
\usage{
simulate_fishmorph_points(
  n_per_species = 15,
  species = c("Species_A", "Species_B", "Species_C"),
  n_replicates = 1,
  curvature = FALSE,
  scale_cm = 1,
  px_per_cm = 12,
  seed = 123
)
}
\arguments{
\item{n_per_species}{Integer, number of individuals simulated per
species. Defaults to \code{15}.}

\item{species}{Character vector of species labels. Defaults to
\code{c("Species_A", "Species_B", "Species_C")}, simulated with
increasingly elongate-to-deep body proportions.}

\item{n_replicates}{Integer, number of digitization replicates
simulated per individual (\code{1} for no replication). Defaults to \code{1}.}

\item{curvature}{Logical, also simulate a 22nd landmark for body
curvature correction of standard length (see \code{\link[=fishmorph_segments]{fishmorph_segments()}}).
Defaults to \code{FALSE} (21 landmarks only, straight-line body length).}

\item{scale_cm}{Numeric, real-world distance in centimetres represented
by the simulated scale bar (landmarks 20-21). Defaults to \code{1}.}

\item{px_per_cm}{Numeric, digitization units per centimetre used to
build the simulated scale bar and body proportions (arbitrary; only
the ratio to body landmark coordinates matters). Defaults to \code{12}.}

\item{seed}{Integer or \code{NULL}. Random seed for reproducibility. Defaults
to \code{123}.}
}
\value{
An object of class \code{"intrait_landmarks"} (see \code{\link[=read_tps]{read_tps()}}),
with 21 (or 22) landmarks per specimen in the fixed order described in
\code{\link[=fishmorph_segments]{fishmorph_segments()}}, and \code{metadata} columns \code{specimen},
\code{individual}, \code{species}, \code{population}, and \code{replicate}. \code{scale} is set
to \code{NULL} (the scale bar is embedded as landmarks 20-21, per the
FISHMORPH scheme, rather than carried as external metadata).
}
\description{
Generates a simulated data set of 21 (or 22) landmarks per specimen,
positioned following the Brosse et al. (2021) FISHMORPH digitization
scheme (see \code{\link[=fishmorph_segments]{fishmorph_segments()}}), for several simulated "species"
with distinct body proportions. This removes any dependency on real
digitized pictures for examples, teaching, and testing of
\code{\link[=fishmorph_segments]{fishmorph_segments()}}, \code{\link[=fishmorph_ratios]{fishmorph_ratios()}}, \code{\link[=trait_space]{trait_space()}}, and
\code{\link[=plot_fishmorph_points]{plot_fishmorph_points()}}.
}
\details{
This landmark scheme mixes true body-shape landmarks (1-19) with an
external scale-bar reference (20-21) and, optionally, a non-homologous
curvature-correction point (22); it is intended for use with
\code{\link[=fishmorph_segments]{fishmorph_segments()}}, not for Generalised Procrustes Analysis
(\code{\link[=gpa_fish]{gpa_fish()}}), which assumes all landmarks are homologous shape
coordinates.
}
\examples{
fish <- simulate_fishmorph_points(n_per_species = 5, n_replicates = 2)
fish
head(fish$metadata)

}
\seealso{
\code{\link[=fishmorph_segments]{fishmorph_segments()}}, \code{\link[=fishmorph_ratios]{fishmorph_ratios()}},
\code{\link[=plot_fishmorph_points]{plot_fishmorph_points()}}
}
