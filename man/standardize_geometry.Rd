% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/standardize_geometry.R
\name{standardize_geometry}
\alias{standardize_geometry}
\title{Standardize landmark scale, scale-bar position, and rotation, without
changing any measurement value}
\usage{
standardize_geometry(
  landmarks,
  specimen = NULL,
  scale_bar_pos = c(0.1, 0.1),
  orient = TRUE
)
}
\arguments{
\item{landmarks}{An object of class \code{"intrait_landmarks"}, or a raw
\verb{p x k x n} landmark array, with at least the 21 landmarks of the
FISHMORPH scheme (points 1-19 plus the scale bar, 20-21).}

\item{specimen}{\code{NULL} (default) to standardize every specimen, or an
integer/character vector to restrict this to a subset.}

\item{scale_bar_pos}{Numeric length-2 vector, the \code{c(x, y)} position (in
the post-rescaling \verb{[0, 1]} space) landmark 20 is moved to; landmark
21 is placed to its right, at \code{scale_bar_pos + c(length, 0)}, where
\code{length} is the scale bar's own original length scaled by the same
factor as the body -- so the calibration ratio between the scale bar
and the body is preserved, only its position and orientation are
standardized. Defaults to \code{c(0.1, 0.1)} (bottom-left).}

\item{orient}{Logical, whether to call \code{\link[=standardize_orientation]{standardize_orientation()}}
first, before steps 1-3, so a specimen digitized mirrored
(left-right) or upside-down (dorsal-ventral) is corrected before
rescaling/rotation rather than needing a separate call. Defaults to
\code{TRUE}, coupling the two functions that earlier versions of this
package's documentation already recommended chaining manually (\verb{fish \\\%>\\\% standardize_orientation() \\\%>\\\% standardize_geometry(orient = FALSE)} is equivalent to the default \code{standardize_geometry(fish)}).
Set to \code{FALSE} if orientation was already standardized separately (as
\code{\link[=correct_geometry]{correct_geometry()}} itself does internally, to stay behaviour
identical across package versions), or if you deliberately want to
preserve each specimen's original mirroring.}
}
\value{
An object of the same class as \code{landmarks}, with every
specimen's coordinates replaced by their standardized version, and,
if \code{landmarks} is an \code{"intrait_landmarks"} object with a \verb{$scale}
element, that element rescaled to match (see Details) so that no
specimen's true real-world size is lost even though every specimen is
now drawn at the same visual size. The returned \code{coords} array carries
a \code{standardization_log} attribute, a \code{data.frame}, one row per
specimen processed, with columns \code{specimen}, \code{scale_factor} (the
isotropic factor applied in step 1), \code{rotation_deg} (the rotation
applied in step 3), \code{y_shift} (the vertical translation applied
immediately after that rotation to bring the axis to \code{Y = 0.5}), and
\code{scale_bar_placed} (logical, whether landmarks 20-21 were
repositioned). Merged with any pre-existing \code{standardization_log} from
an earlier call, so successive calls accumulate a full record. If
\code{orient = TRUE}, the returned object also carries \code{orientation_log}
from the internal \code{\link[=standardize_orientation]{standardize_orientation()}} call (see its own
Return).
}
\description{
The value-preserving half of \code{\link[=correct_geometry]{correct_geometry()}}'s pipeline (its steps
1-3), available on its own: (1) rescale the body landmarks isotropically
so they fit within \verb{[0, 1]} (preserving body shape); (2) reposition the
embedded scale bar (landmarks 20-21) to a fixed corner of that \verb{[0, 1]}
space; (3) rotate the body so the main axis (landmarks 1-2) is exactly
horizontal, landmark 1 to the left of landmark 2, anchored at \code{Y = 0.5}
for every specimen. Because it only ever rescales, translates, and
rigidly rotates coordinates, it never changes any FISHMORPH segment or
ratio value (Euclidean distances, and therefore every ratio computed
from them, are invariant under these operations) -- see Details. This is
in contrast to \code{\link[=correct_geometry_conventions]{correct_geometry_conventions()}} (step 4), which actively
moves landmarks and does change values.
}
\details{
See \code{\link[=correct_geometry]{correct_geometry()}}'s Details for the full rationale behind each of
the three steps (isotropic rescale, scale-bar repositioning, rotation +
vertical anchoring), which this function implements identically -- the
only difference is that step 4 (active correction of landmarks that
still violate the FISHMORPH geometric conventions once the axis is
horizontal) is not performed here; call
\code{\link[=correct_geometry_conventions]{correct_geometry_conventions()}} afterwards for that, or use
\code{\link[=correct_geometry]{correct_geometry()}} directly for the combined pipeline in one call.

As with \code{\link[=correct_geometry]{correct_geometry()}}, running this function twice on
already-standardized data is harmless (idempotent up to floating-point
precision): a specimen already isotropically scaled to \verb{[0, 1]} and
horizontal is left materially unchanged by a second pass.
}
\examples{
fish <- load_t26_saudrune_landmarks()
# orientation (left-right/dorsal-ventral mirroring) is standardized
# automatically first, by default:
fish_std <- standardize_geometry(fish)
attr(fish_std$coords, "orientation_log")
attr(fish_std$coords, "standardization_log")

# equivalent to the pre-existing two-call workflow:
fish_std2 <- standardize_geometry(standardize_orientation(fish), orient = FALSE)

# then, only if/where desired, actively correct remaining conventions:
fish_corrected <- correct_geometry_conventions(fish_std)

}
\seealso{
\code{\link[=correct_geometry_conventions]{correct_geometry_conventions()}} (step 4, which does change
values), \code{\link[=correct_geometry]{correct_geometry()}} (the combined pipeline, unchanged and
still the recommended one-call route for existing workflows),
\code{\link[=standardize_orientation]{standardize_orientation()}}, \code{\link[=correct_landmarks]{correct_landmarks()}},
\code{\link[=plot_fishmorph_points]{plot_fishmorph_points()}}
}
