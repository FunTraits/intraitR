% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary_traits.R
\name{summary_traits}
\alias{summary_traits}
\title{Summarise morphological traits by group}
\usage{
summary_traits(traits, groups)
}
\arguments{
\item{traits}{A numeric \code{data.frame} (or matrix) of traits, one row per
specimen, as returned by e.g. \code{\link[=linear_distances]{linear_distances()}} or
\code{\link[=morpho_ratios]{morpho_ratios()}}. Non-numeric columns are dropped with a warning.}

\item{groups}{A factor or character vector of the same length as
\code{nrow(traits)}, giving the grouping variable.}
}
\value{
A tidy \code{data.frame} with one row per group/trait combination and
columns \code{group}, \code{trait}, \code{n}, \code{mean}, \code{sd}, \code{min}, \code{max}.
}
\description{
Produces a tidy summary (n, mean, standard deviation, min, max) of one
or more morphological traits (e.g. linear distances or ratios), broken
down by a grouping variable such as species or population.
}
\examples{
fish <- simulate_fish_landmarks(n_per_species = 8, n_replicates = 1)
distances <- list(SL = c(1, 7), BD = c(3, 10))
ratios <- morpho_ratios(fish, distances, norm_by = "SL")
summary_traits(ratios[, "BD_ratio", drop = FALSE], fish$metadata$species)

}
\seealso{
\code{\link[=intraspecific_variability]{intraspecific_variability()}}, \code{\link[=morpho_ratios]{morpho_ratios()}}
}
