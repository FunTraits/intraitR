% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trait_disparity.R
\name{trait_disparity}
\alias{trait_disparity}
\title{Test differences in functional trait dispersion between groups}
\usage{
trait_disparity(
  x,
  groups = NULL,
  iter = 999,
  log_transform = TRUE,
  scale = TRUE
)
}
\arguments{
\item{x}{Either an object of class \code{"intrait_traitspace"} (from
\code{\link[=trait_space]{trait_space()}}, built with \code{groups} supplied), or a
\code{data.frame}/matrix of numeric traits (one row per specimen), in
which case \code{groups} must also be supplied and the same
\code{log_transform}/\code{scale} preprocessing as \code{\link[=trait_space]{trait_space()}} is applied
before computing dispersion.}

\item{groups}{Required when \code{x} is a raw trait table; ignored (taken
from \code{x$groups}) when \code{x} is an \code{"intrait_traitspace"} object.}

\item{iter}{Integer, number of random permutations of group labels used
to build the null distribution. Defaults to \code{999}.}

\item{log_transform, scale}{As in \code{\link[=trait_space]{trait_space()}}; only used when \code{x} is
a raw trait table (ignored, and taken from \code{x}, when \code{x} is an
\code{"intrait_traitspace"} object).}
}
\value{
An object of class \code{"intrait_disparity"}, a list with elements
\code{disparity} (named numeric vector of per-group trait variance),
\code{pairwise_diff} (symmetric matrix of observed absolute pairwise
differences in disparity), \code{pairwise_p} (symmetric matrix of
permutation p-values for these differences), and \code{iter}.
}
\description{
Tests whether groups (e.g. species) differ in the multivariate dispersion
of their functional traits, using a permutation approach analogous to
\code{\link[geomorph:morphol.disparity]{geomorph::morphol.disparity()}} but applied to the standardised trait
space built by \code{\link[=trait_space]{trait_space()}} instead of to Procrustes shape
coordinates. For each group, dispersion is measured as trait variance
(the sum of the per-trait variances, i.e. the trace of the group's
trait covariance matrix), computed on the same log-transformed and
standardised data used to build the ordination, so that the result does
not depend on how many axes were retained for plotting. Pairwise
differences in dispersion between groups are tested against a null
distribution obtained by randomly permuting group labels.
}
\details{
The permutation procedure reassigns the \code{n} specimens to groups at
random (preserving observed group sizes), recomputes each group's trait
variance, and derives the null distribution of the pairwise differences
from \code{iter} such permutations plus the observed assignment (the
standard \code{(iter + 1)}-permutation correction; Anderson, 2001). A group
with significantly higher trait variance than another occupies, on
average, a larger region of standardised functional trait space,
consistent with greater morphological or ecological generalism within
that group. Groups with fewer than two specimens receive a disparity of
\code{NA} and are excluded from the permutation test.

This function complements \code{\link[=intraspecific_variability]{intraspecific_variability()}}, which reports
shape disparity (from Procrustes coordinates) and univariate
coefficients of variation, but does not test for group differences in
the dispersion of a multivariate \emph{trait} space.
}
\examples{
fish <- simulate_fishmorph_points(n_per_species = 12, n_replicates = 1)
segments <- fishmorph_segments(fish)
ratios <- fishmorph_ratios(segments)
ts <- trait_space(ratios, groups = fish$metadata$species)
\donttest{
td <- trait_disparity(ts, iter = 199)
td
}

}
\references{
Anderson MJ (2001). A new method for non-parametric multivariate
analysis of variance. Austral Ecology, 26(1), 32-46.
\doi{10.1111/j.1442-9993.2001.01070.pp.x}
}
\seealso{
\code{\link[=trait_space]{trait_space()}}, \code{\link[=intraspecific_variability]{intraspecific_variability()}}
}
