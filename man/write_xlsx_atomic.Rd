% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/landmark_journal.R
\name{write_xlsx_atomic}
\alias{write_xlsx_atomic}
\title{Write a workbook atomically}
\usage{
write_xlsx_atomic(x, path, keep_prev = TRUE)
}
\arguments{
\item{x}{A named list of \code{data.frame}s (one per sheet), as
\code{\link[writexl:write_xlsx]{writexl::write_xlsx()}} takes.}

\item{path}{Target path.}

\item{keep_prev}{Keep the previous generation (default \code{TRUE}).}
}
\value{
\code{TRUE}, invisibly, if the write succeeded.
}
\description{
\code{\link[writexl:write_xlsx]{writexl::write_xlsx()}} overwrites its target in place: while it is being
rewritten (seconds, for a workbook of several Mb) the file is in an
intermediate state, and an interruption destroys it. This writes to a
temporary file in the SAME directory -- a necessary condition for the rename
to be atomic, a cross-volume rename being in fact a copy -- then switches by
renaming.
}
\details{
The old file is not deleted but moved to \code{"<name>.prev.xlsx"}, which gives a
one-generation backup for free. If the final rename fails, the old file is
restored.
}
\examples{
f <- file.path(tempdir(), "demo_atomic.xlsx")
if (requireNamespace("writexl", quietly = TRUE)) {
  write_xlsx_atomic(list(measurements = data.frame(specimen = "fish_01")), f)
  file.exists(f)
}
unlink(c(f, sub("\\\\.xlsx$", ".prev.xlsx", f)))

}
\seealso{
\code{\link[=consolidate_landmarks]{consolidate_landmarks()}}, \code{\link[=digitize_landmarks]{digitize_landmarks()}}
}
