# Print an `"intrait_disparity"` object

Print an `"intrait_disparity"` object

## Usage

``` r
# S3 method for class 'intrait_disparity'
print(x, ...)
```

## Arguments

- x:

  An object of class `"intrait_disparity"`, as returned by
  [`trait_disparity()`](https://funtraits.github.io/intraitR/reference/trait_disparity.md).

- ...:

  Currently unused.

## Value

Invisibly returns `x`.
